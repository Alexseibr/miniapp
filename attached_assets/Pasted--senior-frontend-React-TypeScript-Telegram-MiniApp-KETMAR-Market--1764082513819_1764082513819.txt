Ты — senior frontend-разработчик (React + TypeScript, Telegram MiniApp) в проекте KETMAR Market.

ПРОБЛЕМА:
На шаге «Фото товара» (Подача объявления) сейчас есть только 4 пустых слота «Добавить» и подсказка про фото.  
Шаг помечен как «До 4 фотографий (необязательно)», но:

❌ нет кнопки, чтобы перейти на следующий шаг БЕЗ фото,  
❌ мастер блокируется, если пользователь не хочет/не может добавить фото.

НУЖНО:

Сделать этот шаг по-настоящему **опциональным** и добавить явную кнопку перехода дальше.

========================================
1. НАЙТИ КОМПОНЕНТ ШАГА «ФОТО ТОВАРА»
========================================

Найди компонент, который рендерит экран как на скриншоте:

- заголовок: «Фото товара»
- подзаголовок: «До 4 фотографий (необязательно)»
- 4 серых слота «Добавить»
- снизу синий инфоблок “Качественные фотографии помогают быстрее продать товар”.

Это может быть, например:

- `AdCreatePhotosStep.tsx`
- `CreateAdPhotosScreen.tsx`
- `PhotosStep.tsx`

Этот компонент должен получать что-то вроде:

- `onNext()` / `onComplete()` / `goToStep(...)` из мастера,
- state с массивом фото (`images`, `files` и т.п.).

========================================
2. ДОБАВИТЬ КНОПКУ «ПРОДОЛЖИТЬ БЕЗ ФОТО»
========================================

ВНИЗУ экрана (над таббаром, рядом с зоной, где сейчас только инфоблок) нужно:

1) Основная кнопка:
   - текст: `Продолжить`
   - поведение:
     - если есть хотя бы одно фото — работает как сейчас (сохраняет фото и идёт на следующий шаг),
     - если фото нет — тоже идёт на следующий шаг, БЕЗ ошибок.

2) Если логика мастера подразумевает отдельную кнопку для “пропуска”, можно сделать так:

- основная синяя кнопка:
  - `Продолжить` (если есть фото)
- под ней текстовая/вторичная кнопка:
  - `Продолжить без фото`
  - она **всегда** доступна и просто сразу вызывает `onNext()` без попыток загрузки.

Но минимально достаточно ОДНОЙ большой кнопки `Продолжить`, которая не требует фото.

Пример JSX:

```tsx
<div className="photos-step-footer">
  <PrimaryButton
    fullWidth
    onClick={handleContinue}
  >
    Продолжить
  </PrimaryButton>
</div>
Где:

ts
Копировать код
const hasImages = images.length > 0;

const handleContinue = async () => {
  try {
    if (hasImages) {
      // существующая логика загрузки/подготовки фото
      await uploadImagesIfNeeded();
    }
    // В ЛЮБОМ СЛУЧАЕ:
    // сохранить в форму, что фото может быть пустым массивом
    onNext(); // или onComplete()
  } catch (e) {
    // если загрузка фото упала — показать ошибку, НО:
    // НЕ трогать возможность продолжить без фото, если пользователь очистит массив
  }
};
========================================
3. УБЕДИТЬСЯ, ЧТО ВАЛИДАЦИЯ НЕ ТРЕБУЕТ ФОТО
Проверь валидацию мастера/формы:

нет ли правил типа:

minImages: 1

photos как required.

Если есть — убрать/сделать опциональными:

photos должно быть либо пустым массивом [], либо не передаваться вообще,

backend тоже НЕ должен возвращать 400/422 из-за отсутствия фото.

Если где-то есть Zod/Yup-схема — проверь, что photos не required(), а optional().

========================================
4. ПОВЕДЕНИЕ ПРИ ОШИБКЕ ЗАГРУЗКИ
Если при наличии фото не удалось загрузить файл (ошибка получения upload URL и т.п.), логика должна быть такой:

показать красный блок ошибки (как сейчас),

НО если пользователь:

удалит все фото,

и нажмёт Продолжить,

→ шаг всё равно должен завершаться и переходить дальше без фото.

То есть ошибки загрузки не должны блокировать переход в случае, когда images.length === 0.

========================================
5. ФИНАЛЬНАЯ ПРОВЕРКА
Проверь 3 сценария:

Пользователь добавил 1–4 фото → нажал Продолжить → фото загрузились → переход на следующий шаг.

Пользователь НЕ добавлял фото → нажал Продолжить → сразу переход на следующий шаг, без предупреждений.

Фото были, но загрузка упала:

показали ошибку,

пользователь удалил фото и нажал Продолжить → шаг завершился, объявление создаётся дальше без фото.

========================================
6. В КОНЦЕ — КОРОТКИЙ ОТЧЁТ
В конце:

перечисли изменённые файлы, например:

AdCreatePhotosStep.tsx — добавлена кнопка «Продолжить», логика handleContinue, шаг стал опциональным.

adCreateValidation.ts — убрано обязательное требование фото.

предложи ветку и коммит:

ветка: fix/photos-step-optional

коммит: fix(ad-create): make photos step optional and add continue button without images.

makefile
Копировать код
