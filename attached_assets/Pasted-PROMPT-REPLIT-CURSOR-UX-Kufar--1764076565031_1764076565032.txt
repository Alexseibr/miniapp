PROMPT ДЛЯ REPLIT / CURSOR
Новый главный UX: нижнее меню + радиус (без карты, без копирования Kufar)

Скопируй всё между text и в Replit / Cursor:

Ты — senior frontend engineer (React + TypeScript + Telegram MiniApp) и при необходимости backend developer.
Нужно ПЕРЕДЕЛАТЬ основной UX маркетплейса LOCALHUB / KETMAR так, чтобы он был:

- проще, чем Kufar,
- стилистически ближе к Яндекс/простым сервисам,
- понятен даже пожилым людям.

Главная идея:
1) Нижняя навигация с вкладками:
   - Главная
   - Лента (или "Рядом")
   - Мои
   - Избранное
   - Профиль

2) Во всех сценариях **главная ось — РАВНО расстояние (радиус от пользователя)**, а уже потом категории/группы.

3) На "Главной" — поиск по конкретной ГРУППЕ (например, Недвижимость → Квартиры → 1-комнатные) с колесиком радиуса.

4) На вкладке "Лента" — общий поток объявлений "Рядом со мной" по расстоянию, с фильтрами по группам *дополнительно*.

===========================================================================  
1. КОНТЕКСТ ПРОЕКТА (MINIAPP + BACKEND)  
===========================================================================

Перед началом:

1) найди MiniApp (скорее всего `miniapp/` или похожая папка),
2) найди текущую главную страницу (HomePage / MainPage / AdsPage) и существующие фильтры (скопированные с Kufar),
3) найди API-клиент для объявлений (например `miniapp/src/api/ads.ts`),
4) убедись, что backend уже умеет geo-поиск:
   - GET /api/ads/nearby?lat=...&lng=...&radiusKm=...
   - и возвращает массив объявлений с полем `distanceKm` (если нет — добавь на backend).

Наша задача — **упростить UX**, НЕ копировать сильно Kufar, а сделать свой, заточенный под гео.

===========================================================================  
2. НИЖНЕЕ МЕНЮ (ТАБ-БАР)  
===========================================================================

Сделай (или доработай) низ экрана MiniApp:

- 5 вкладок:
  - "Главная"
  - "Лента" (можно назвать "Рядом")
  - "Мои"
  - "Избранное"
  - "Профиль"

Требования:

- это стандартный таб-бар снизу,
- активная вкладка подсвечена,
- иконки простые и понятные (домик, список, человек, сердечко, шестерёнка/профиль),
- крупные подписи под иконками.

Маршрутизация:

- "Главная" → новый экран `MainCategoryScreen` (описан ниже),
- "Лента" → новый экран `NearbyFeedScreen`,
- "Мои" → мои объявления,
- "Избранное" → избранные объявления,
- "Профиль" → профиль/настройки.

===========================================================================  
3. ХУК ГЕОПОЗИЦИИ (ЕГО НУЖНО ИСПОЛЬЗОВАТЬ ВО ВСЕХ ГЕО-СЦЕНАРИЯХ)  
===========================================================================

Создай/доработай хук `useGeoLocation()`:

`miniapp/src/hooks/useGeoLocation.ts` (если ещё нет):

- возвращает:
  ```ts
  interface GeoCoords { lat: number; lng: number; }

  interface UseGeoLocationResult {
    coords: GeoCoords | null;
    loading: boolean;
    error: string | null;
    requestLocation: () => void;
  }


requestLocation():

использует navigator.geolocation.getCurrentPosition,

при ошибке — выставляет error (но не ломает UI),

coords сохраняет в стейт.

Этот хук будет использован и на "Главной", и на "Ленте".

===========================================================================
4. ЭКРАН "ГЛАВНАЯ" — ВЫБОР ГРУППЫ + РАДИУС

Это первый экран, когда пользователь заходит.

Логика:

Верхняя часть — выбор группы/сегмента:

крупные кнопки типа:

Недвижимость

Авто

Товары

Услуги

Продукты/фермеры

можно сделать в виде сетки из 2 столбцов или ряда крупных иконок.

названия, иконки и ID категорий должны подтягиваться с backend (или статически, если уже есть).

После выбора группы (например, Недвижимость) отображается:

строка подгрупп (подкатегорий):

[Квартиры]

[Комнаты]

[Дома]

[Участки]

или следующий уровень "Квартиры → 1-комнатные / 2-комнатные" (в зависимости от структуры категорий в базе).

Ниже — блок радиуса:

Заголовок:

"Поиск объявлений вокруг вас"

Компонент в виде колёсика/ползунка:

можно реализовать как:

<input type="range"> (с крупным стилизованным ползунком),

или ряд крупных пресет-кнопок: 1 км, 2 км, 3 км, 5 км, 10 км, 15 км, 30 км.

Рядом крупный текст:

"Радиус: 3 км" / "Радиус: 500 м" (если < 1 км).

Должно быть элементарно понятно: влево — меньше, вправо — больше.

Центральная область — список объявлений по выбранной группе и радиусу:

как только есть coords + выбранная группа + установленный radiusKm:

вызываем API /api/ads/nearby:

с параметрами: lat, lng, radiusKm, category/group/....

в ответе получаем список объявлений.

показываем их в виде списка (AdCard), упорядоченного по расстоянию (от ближе к дальше).

Случай, когда объявлений мало:

если в списке:

0 объявлений → показать:

"В радиусе {radiusKm} км объявлений не найдено."

крупную кнопку: "Увеличить радиус", которая:

либо автоматически ставит radiusKm + 5 км,

либо прокидывает фокус к колесику радиуса.

1–2 объявления → можно дополнительно мягко подсказать:

"Нашли мало объявлений. Попробуйте увеличить радиус."

Взаимосвязь с колесиком:

при прокручивании (увеличении/уменьшении радиуса) список объявлений перезагружается.

обязательно добавить debounce (например, 300–500 мс) перед запросом на backend, чтобы не спамить API.

при изменении радиуса:

текст "Радиус: X км" обновляется,

если объявлений мало, подсказка остаётся.

===========================================================================
5. ВКЛАДКА "ЛЕНТА" — ЛЕНТА «РЯДОМ» С РАДИУСОМ ВВЕРХУ

Это второй сценарий: человек хочет просто посмотреть всё, что рядом вообще, а не по конкретной группе.

Экран NearbyFeedScreen (вторая вкладка):

Верхняя часть — управление радиусом:

такой же или похожий UI, как на "Главной":

либо слайдер (range),

либо ряд кнопок-пресетов (1 / 3 / 5 / 10 / 15 / 30 км),

всегда крупная надпись:

"Показать объявления в радиусе: X км".

Под ним — кнопка "Фильтры":

Кнопка "Фильтры" или "Выбрать группу" (крупная).

По нажатию:

открывается простая панель/модалка с выбором групп:

чекбоксы или чипы:

[Все группы] (по умолчанию)

Недвижимость

Авто

Продукты

Услуги

и т.п.

можно реализовать несколькими крупными кнопками с иконками.

Выбранные группы:

отправляются в запрос к backend как массив ID/slug’ов,

либо как один categoryId, если логика backend сейчас только по одному.

Основная часть экрана — лента объявлений рядом:

заголовок:

"Объявления рядом с вами"

список объявлений (AdCard), отсортированный по расстоянию.

в каждой карточке ОБЯЗАТЕЛЬНО:

название,

цена,

distanceKm, красиво отформатированная:

< 1 км → "📍 850 м от вас",

>= 1 км → "📍 2.3 км от вас".

Логика загрузки:

как только есть coords и radiusKm:

делаем запрос /api/ads/nearby без конкретной категории (или с выбранными фильтрами),

подставляем lat, lng, radiusKm, categoryIds[] / filter.

при изменении радиуса или фильтров → перезагружаем ленту (с debounce).

Если нет гео (пользователь не дал доступ):

большой текст:

"Чтобы показать объявления рядом, нужно определить ваше местоположение."

большая кнопка:

"Определить местоположение"

вызывает requestLocation() из useGeoLocation().

===========================================================================
6. ВСПОМОГАТЕЛЬНЫЕ ДЕТАЛИ (UX ДЛЯ ПОЖИЛЫХ)

Очень важно:

Шрифты и элементы:

не делать мелкий текст,

кнопки и ползунки — достаточно крупные, легко попасть пальцем.

Формулировки:

избегать жаргона и сложных терминов:

"Радиус" + простое объяснение (можно подпись: "на каком расстоянии от вас искать объявления"),

"Увеличьте радиус — объявлений станет больше".

Состояния загрузки:

при запросе показывать явно:

"Загружаем объявления…" (можно со спиннером),

при ошибке:

"Не удалось загрузить объявления. Попробуйте ещё раз."

Объявления:

карточки не перегружать мелкими элементами,

ключевые поля: название, цена, расстояние, мини-фото.

===========================================================================
7. РЕФАКТОРИНГ: УБРАТЬ СЛИШКОМ СЛОЖНЫЙ ФИЛЬТР KUFaR-СТИЛЯ

Если сейчас на главной/поиске используется сложная панель фильтров а-ля Kufar:

НЕ УДАЛЯЙ код полностью, но:

вынеси его в отдельный модуль/экран, если нужно сохранить,

по умолчанию новая главная и "Лента" должны использовать упрощённые:

выбор группы,

один ползунок/колесо радиуса,

простые фильтры.

Цель: человек не должен видеть 100 параметров сразу.

===========================================================================
8. ТЕХНИЧЕСКИЕ МОМЕНТЫ

TypeScript:

без any, нормальная типизация пропсов, стейта.

API:

централизуй вызовы /api/ads/nearby в одном месте (API-клиент),

убедиcь, что туда передаются:

lat, lng, radiusKm,

group/category/filters.

Перформанс:

используй debounce для запросов на изменение радиуса.

Telegram WebApp:

проверь, что низ экрана с таб-баром и контентом не уходит за рамки,

учти WebApp.viewportHeight.

===========================================================================
9. В КОНЦЕ — ОТЧЁТ О РЕЗУЛЬТАТАХ

Когда всё реализуешь:

Выведи список изменённых/новых файлов.

Для каждого — 1–2 строки, что было сделано.

Предложи ветку и коммит:

ветка: feature/simple-geo-home-and-feed

коммит: feat(ui): add bottom nav, radius-based home and nearby feed