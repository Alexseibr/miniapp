ПРОМТ ДЛЯ REPLIT — ЛЕНТА v1.3 LITE (1 фото, старый вид текста, бесконечная карусель + новые объявления)

Ты — senior full-stack разработчик (React + Node.js + MongoDB, Telegram MiniApp).
В проекте уже есть рабочая TikTok-лента (FeedScreen):

один товар на экран,

вертикальный свайп вверх/вниз,

показываются фото, цена, название, описание, расстояние,

есть лайк/дизлайк/крестик,

используется /api/feed и user_feed_events.

Нужно упростить и улучшить Ленту, но:

вернуть визуал как в самой первой версии,

оставить бесконечную карусель вверх/вниз с подмешиванием новых объявлений,

и улучшить скорость загрузки фотографий.

1. Одна “главная” фотография в ленте

В Ленте показываем только одну фотографию объявления:

берём images[0] как главную.

Никакого листания фото влево/вправо в Ленте нет.

Во внутренней карточке объявления (при открытии) всё как раньше:
там можно листать все фото, ничего не менять.

2. Вернуть вид карточки как в ПЕРВОЙ версии

Нужно, чтобы карточка в Ленте выглядела максимально так же, как до TikTok-версии, то есть:

Название товара

Отображается сверху над фото (или в верхней части карточки, как было изначально).

Жирный шрифт, хорошо читаемый.

Отступы, размер текста и стиль — как в нашей первой успешной реализации.

Фото

Одна картинка images[0], по центру, object-fit: cover, на всю доступную ширину, с фиксированной высотой.

Цена

Сразу под фото, крупная и жирная.

Стиль и расположение как в первой версии, без экспериментов.

Описание и расстояние

Под ценой:

краткое описание (1–2 строки),

расстояние до продавца.

Формат расстояния:

📍 Брест (Ковалёвка) · 1,3 км

город + район (если есть) + точка + расстояние с одним знаком после запятой.

В итоге карточка должна ощущаться как “инста-пост” в столбик, но с нашим первым знакомым UI.

3. Убрать крестик и дизлайк, оставить только сердечко ❤️

Полностью убрать из Ленты:

кнопку ❌,

кнопку 👎 (дизлайк).

Оставить одну кнопку лайка:

при нажатии:

меняется визуальное состояние (заполненное сердце / цвет),

отправляется событие like на backend (используй уже существующую аналитику).

Расположение лайка:

рядом с ценой (справа от цены или под ней — выбери аккуратный вариант),

чтобы всё выглядело гармонично с версткой первой версии.

4. Плавная анимация смены карточек (вверх/вниз)

Сейчас карточки “щёлкаются” резко. Нужно:

При свайпе вверх:

текущая карточка плавно уезжает вверх (translateY(-100%)),

следующая поднимается снизу (translateY(100% → 0)).

При свайпе вниз:

текущая карточка уезжает вниз,

предыдущая заезжает сверху.

Технически:

оставить существующую логику смены currentIndex и событий scroll_next / scroll_prev,

обернуть рендер карточки в анимационный контейнер:

CSS transitions,

либо Framer Motion / React Spring (если уже подключены).

Важно: анимация не должна ломать:

индексы,

аналитику impression / dwellTime,

обработчики свайпов.

5. Подсказка “свайпай вверх/вниз” при первом входе

Проблема: пользователи не понимают, что Лента свайпается.

Решение:

При первом заходе в Ленту:

показать поверх карточки небольшой overlay:

стрелки ↑ ↓,

текст: Свайпайте вверх и вниз, чтобы листать объявления.

Overlay скрывать:

после первого свайпа, или

автоматом через 2–3 секунды.

Сохранять флаг “подсказка показана”:

в localStorage или в persistent-сторе, привязанном к userId,

чтобы в следующий раз подсказка не показывалась.

6. Бесконечная карусель + подмешивание новых объявлений

Этот пункт принципиальный.

Требование:

Лента должна быть бесконечной:

пользователь может листать и вверх, и вниз без “конца списка”.

Новые объявления в его радиусе должны подмешиваться вперёд:

если где-то рядом появилось новое объявление,

через несколько свайпов оно попадёт в Ленту “вперёд по ходу движения”.

6.1. Базовая структура состояний

В FeedScreen заведи:

items: Ad[] — текущий пул объявлений,

currentIndex: number — индекс текущей карточки,

history: Ad[] (опционально) — если удобнее хранить историю отдельно,

newBuffer: Ad[] — буфер новых объявлений, пришедших позже.

6.2. Подгрузка новых объявлений

Периодически (например, каждые 30–60 секунд) запрашивай backend:

либо отдельный endpoint GET /api/feed/new?since=<lastSeenCreatedAt>&radiusKm=20,

либо /api/feed с параметрами since/afterId / нужным курсором.

Backend должен вернуть объявления, созданные ПОСЛЕ последнего известного времени, в том же радиусе гео.

На фронте:

фильтруем только объявления с фото (images.length > 0),

удаляем дубликаты по id,

складываем в newBuffer.

6.3. Листание ВВЕРХ (вперёд)

При свайпе вверх:

Увеличиваем currentIndex.

Если в newBuffer есть данные и мы продвигаемся вперёд:

подмешиваем 1–2 объявления из newBuffer в items “где-то впереди”, например сразу после текущего индекса;

очищаем использованные элементы из newBuffer.

Если currentIndex подходит к концу items:

подгружаем ещё партию из /api/feed (как обычная пагинация),

можно перемешать их random-ом, чтобы порядок вперёд был не строго один и тот же.

Идея: вперёд пользователь видит поток, куда мягко влетали новые объявления.

6.4. Листание ВНИЗ (назад)

При свайпе вниз:

Если currentIndex > 0:

уменьшаем currentIndex,

показываем конкретно предыдущий элемент items[currentIndex - 1]

(то есть всегда можно вернуться к тому, что уже видел).

Если currentIndex == 0:

можно сделать wrap-around:

перемотать на конец массива (currentIndex = items.length - 1),

чтобы лента ощущалась “по кругу”.

Важное правило:

Вниз — это история (то, что уже видел).

Вверх — новые/ещё не просмотренные (включая подмешанные новые объявления и “старый хвост”).

7. Оптимизация скорости загрузки фотографий

Сейчас фотки грузятся заметно медленно. Нужно сделать так, чтобы:

при листании карточка почти сразу показывала картинку (как в TikTok/Instagram),

не было белых пустых экранов.

7.1. Использовать уменьшенные превью

Если у нас уже есть поля preview / thumbnail / smallImageUrl:

в Ленте используй именно их,

а полные версии оставь только для детальной карточки объявления.

Если таких полей пока нет:

на backend добавить генерацию уменьшенной версии при загрузке фото и хранить URL как previewUrl,

в ответе /api/feed добавить поле previewUrl рядом с images.

7.2. Прелоад соседних карточек

При загрузке текущей карточки:

создавай new Image().src = next.previewUrl (и предыдущей, если есть),

чтобы браузер кэшировал картинку заранее.

7.3. Ленивая загрузка + skeleton

Для <img>:

loading="lazy",

заранее заданная высота, чтобы не прыгал layout,

пока картинка не загрузилась — показывать:

простой серый/размытый блок,

либо skeleton с лёгким градиентом.

8. Что НЕЛЬЗЯ ломать

Оставить как есть:

Endpoint /api/feed и основной контракт (можно расширить, но не ломать).

Схему и логику user_feed_events:

impression,

scroll_next / scroll_prev,

view_open,

like.

Детальную карточку объявления (где все фото, описание, контакты).

Нижнее меню, навигацию и остальные экраны приложения.

9. Чек-лист после реализации

После правок проверь:

Лента показывает:

название сверху,

одну фотку,

цену под фото,

под ней краткое описание и расстояние.

Крестика и дизлайка нет, только ❤️.

Свайпы вверх/вниз — плавные.

При первом входе появляется подсказка “свайпайте вверх/вниз” и исчезает.

Можно листать и вверх, и вниз очень долго:

вниз → возвращаюсь к уже виденным карточкам,

вверх → вижу новые, в том числе те, что появились недавно.

Фото появляются быстро, практически без белых экранов.

Клик по фото открывает детальную карточку, выход возвращает в то же место Ленты.

Аналитика продолжает отправляться без ошибок.

Сделай, пожалуйста, эти изменения аккуратно, поверх существующей реализации, с минимальными вмешательствами в архитектуру.