PROMPT 2 — Полный редизайн главной страницы под «быстрый маркет»
Хочу, чтобы ты ПОЛНОСТЬЮ ПЕРЕРАЗРАБОТАЛ главную страницу MiniApp KETMAR Market под концепцию «быстрый маркет» (только быстрые категории: личные вещи, фермерский рынок, сельхоз техника, услуги, аренда, электроника б/у).

Важно:
– НЕ трогай backend-API.
– Работай с frontend MiniApp (React/TS), компонент Home/Главная и связанными с ним подкомпонентами.
– У нас уже есть флаг category.visible — показывать только видимые категории.

ЦЕЛЬ: Главный экран должен быть максимально простым, как «витрина быстрого барахолочного маркета»:
1) Сразу видно: где я нахожусь (район/деревня),
2) Что могу найти (поиск),
3) Какие основные быстрые категории есть.

========================================
1. СТРУКТУРА НОВОЙ ГЛАВНОЙ
========================================

Разбей главный экран на 4 блока сверху вниз:

(1) Блок локации:
– Маленький чип: «Вы в: <название района/деревни>»
– Кнопка-иконка рядом: «Изменить» (открывает выбор локации / карта).
– Всё компактно, без лишнего воздуха.

(2) Поисковая строка:
– На всю ширину с отступами (16px).
– Стиль iOS: закругление 16–20, светлый фон, иконка лупы.
– Placeholder: «Что вы ищете рядом с вами?»
– При вводе и нажатии Enter:
  → сразу переходим на экран Ленты (Feed) во вкладку «Лента» с query + радиус по умолчанию 3 км.

(3) Быстрые категории (основной блок):
– Список категорий в 2 или 3 колонки (лучше 3, если красиво помещается).
– Карточка категории:
  - большая иконка/плашка,
  - название: «Фермерский рынок», «Личные вещи», «Электроника б/у», «Сельхоз техника», «Услуги», «Аренда».
  - маленький бейдж: количество объявлений рядом (например «23 рядом»).
– Клик по категории:
  → переход в Ленту с фильтром по этой категории + радиус по умолчанию 3 км.

(4) Блок «Что популярно рядом»:
– Небольшая горизонтальная лента:
  - заголовок: «Популярно рядом с вами»
  - карточки: по 1–2 товара из разных категорий (top по просмотрам за 24 часа).
– Карточка мини:
  - фото,
  - цена,
  - расстояние.

========================================
2. КАТЕГОРИИ, КОТОРЫЕ ДОЛЖНЫ БЫТЬ ВИДИМЫ
========================================

Используй только категории с visible=true, но на главной отобрази приоритетно (в таком порядке):

1) Фермерский рынок  
2) Личные вещи  
3) Электроника б/у  
4) Сельхоз техника  
5) Услуги  
6) Аренда

Если категория ещё не заведена в конфиге — можешь создать её в client-side mock, но логика должна опираться на наши реальные Category-модели.

========================================
3. УПРОЩЁННЫЙ UX
========================================

– Убери глубокую вложенность: на главной только верхний уровень категорий.
– Никаких сложных фильтров на главной.
– Любое действие (поиск / выбор категории) всегда ведёт в «Ленту» (отдельный экран) с уже подставленными:
  - геокоординатами пользователя,
  - радиусом,
  - категорией (если выбрана),
  - текстом поиска (если введён).

========================================
4. АДАПТИВНОСТЬ
========================================

– Проверь, чтобы дизайн корректно работал на маленьких и больших iPhone.
– Ничего не должно прилипать к краям, текст не должен уходить в две строки слишком рано.
– Используй существующую дизайн-систему проекта (цвета, отступы, шрифты).

Сделай редизайн главной по описанию выше, замени текущую реализацию на новую, сохранив существующую навигацию по вкладкам (Главная / Лента / Мои / Избранное / Профиль).

🟦 PROMPT 3 — AI-настройка умной сортировки «быстрых» товаров
Нужно реализовать УМНУЮ СОРТИРОВКУ объявлений для «быстрого маркета».

Цель:
– По умолчанию показывать те товары, которые НАИБОЛЕЕ ВЕРОЯТНО пользователь купит рядом с собой.
– При этом учитывать: расстояние, свежесть, активность просмотров, клики в контакты, сохранения в избранное.

РАБОТАЕМ:
– ТОЛЬКО на backend (Node/TS, сервисы выборки объявлений).
– Не ломаем существующие API, а дорабатываем логику сортировки / сервис уровня поиска.

========================================
1. ВВЕСТИ СКОPИНГ ДЛЯ ОБЪЯВЛЕНИЙ
========================================

Добавь в сервис выборки объявлений helper-функцию:

calculateFastMarketScore(ad, { userLocation, now }) → number

Формула должна учитывать:

1) Расстояние:
– Чем ближе, тем больше вес.
– Пример: scoreDistance = max(0, 1 - (distanceKm / radiusKmMax))

2) Свежесть:
– Новые объявления должны подниматься выше.
– Пример: scoreFresh = exp(- ageInHours / 48)

3) Вовлечённость:
– Просмотры за последние 24/72 часа,
– Количество переходов к контактам,
– Количество добавлений в избранное.

Пример:
scoreEngagement = normalize(viewsLast72h * a + contactClicks * b + favorites * c)

4) Тип категории:
– Фермерский рынок и Личные вещи можно подсвечивать чуть выше (быстрые продажи).
– Введи categoryBoost:
  - +10% для "фермерский рынок",
  - +5% для "личные вещи",
  - 0 для остальных.

Итоговый балл:
fastMarketScore = w1 * scoreDistance + w2 * scoreFresh + w3 * scoreEngagement + categoryBoost

Конкретные веса:
– w1 (distance) = 0.4
– w2 (fresh) = 0.3
– w3 (engagement) = 0.3

========================================
2. ИНТЕГРАЦИЯ В /api/ads/nearby и /api/ads/search
========================================

– Для эндпоинтов, где sort не задан явно, используем:
  sort = «fastMarketScore DESC»

– Если пользователь выбрал сортировку:
  - «По расстоянию» → сортируем только по distanceKm.
  - «По дате» → сортируем только по createdAt DESC.
  - «По популярности» → сортируем по fastMarketScore DESC.

========================================
3. СБОР СТАТИСТИКИ ДЛЯ СКОPИНГА
========================================

Нужно убедиться, что в модели Ad или отдельной таблице есть поля:

- viewsCount (общие просмотры),
- viewsLast24h,
- viewsLast72h,
- contactClicks,
- favoritesCount.

Если нет — добавить:
– модель/коллекцию AdStats с привязкой по adId,
– сервис инкремента статистики при:
  - просмотре карточки,
  - клике по телефону/username,
  - добавлении в избранное.

========================================
4. КЭШИРОВАНИЕ
========================================

Так как fastMarketScore может считаться дорогим:
– Добавь поле lastFastMarketScore и lastFastMarketScoreAt (в Ad или AdStats),
– Раз в N минут (например, 5–10) пересчитывай score асинхронно (cron/worker),
– В онлайновых запросах используй уже подготовленный score.

========================================
5. НЕ ЛОМАЕМ СУЩЕСТВУЮЩИЕ КЛИЕНТЫ
========================================

– API-ответы должны остаться в том же формате.
– Добавь в ответ дополнительное поле:
  - fastMarketScore (number)
  - distanceKm (если не было)

По умолчанию front сможет использовать только distanceKm и sort, но backend уже будет выдавать более релевантный список.

Реализуй описанный scoring, интегрируй в /api/ads/nearby и /api/ads/search, не меняя схемы ответа (кроме добавления новых необязательных полей).

🟦 PROMPT 4 — Авто-подсказки для фермеров («сегодня выгодно продавать картошку»)
Нужно реализовать БЭКЕНД-ЛОГИКУ "подсказок фермеру":
– Анализировать спрос/поиск по продуктам фермерского рынка,
– Понимать, что в районе вырос интерес к определённому товару (картошка, малина, яйца и т.п.),
– Отправлять фермерам подсказки вида:
  «В вашем районе ищут картошку — разместите объявление».

РЕАЛИЗУЕМ ТОЛЬКО backend:
– сервис аналитики,
– хранение аггрегатов,
– генерация рекомендаций для фермеров.
(Отправка пушей/бота можно оформить как вызов обработчика/хелпера.)

========================================
1. ЛОГИРОВАТЬ ПОИСКОВЫЕ ЗАПРОСЫ ПО ФЕРМЕРКЕ
========================================

Создай коллекцию/таблицу SearchLog:

- id
- userId/telegramId (опционально)
- queryText
- normalizedTokens[]   // ['картошка', 'куплю']
- location { lat, lng, regionId }
- createdAt

Нас интересуют запросы по еде/фермерским товарам.

Сделай словарь ключевых слов:
– картошка, морковь, свекла, лук, капуста, яблоки, груши, клубника, малина, черника, смородина, мёд, яйца, молоко, творог, сыр, зелень, выпечка, хлеб, пироги и т.п.

При поиске:
– если запрос содержит фермерские ключевые слова → логируем в SearchLog.

========================================
2. АГРЕГАЦИЯ СПРОСА ПО РАЙОНУ
========================================

Создай периодический job (cron/worker) раз в 30–60 мин:

1) Группируем SearchLog по:
   - regionId (или округление по координатам),
   - normalizedProduct (картошка, малина, мёд и т.д.),
   - период (последние 24 часа / 7 дней).

2) Считаем:
   - countSearches24h,
   - countSearches7d,
   - тренд (рост/падение по сравнению с предыдущими 7 днями).

Результат сохраняем в таблицу FarmerDemand:

- id
- regionId
- productKey (картошка, малина, яйца…)
- searches24h
- searches7d
- trend (up/down/flat)
- updatedAt

========================================
3. НАХОДИТЬ АКТУАЛЬНЫХ ФЕРМЕРОВ В РАЙОНЕ
========================================

Фермер — это продавец, у которого:

– либо есть активные объявления в категории "Фермерский рынок" в этом регионе,
– либо он указал себя как фермер в профиле (farmer=true),
– либо исторически продавал фермерские товары (Ad.category=фермерский рынок).

Нужен сервис:
getFarmersInRegion(regionId) → список farmerUserIds/telegramIds.

========================================
4. ГЕНЕРАЦИЯ ПОДСКАЗОК
========================================

Создай таблицу FarmerSuggestion:

- id
- farmerId
- regionId
- productKey
- message
- status: 'pending' | 'sent' | 'clicked'
- createdAt
- sentAt (опционально)

Cron job (раз в 1 час):

для каждой записи FarmerDemand, где:
– searches24h > некого порога (например, 5–10 запросов),
– trend = 'up'

делает:

1) Находим всех фермеров в этом регионе.
2) Для каждого проверяем, не было ли подсказки по этому productKey за последние N дней.
3) Создаём FarmerSuggestion со следующим текстом:

Примеры сообщений:
– «В вашем районе вырос спрос на картошку. Разместите объявление и продайте быстрее.»
– «Пользователи рядом ищут малину. Если у вас есть ягоды — самое время их выставить.»
– «В радиусе 3 км люди ищут яйца/молоко. Добавьте объявление в раздел "Фермерский рынок".»

========================================
5. ИНТЕГРАЦИЯ С PUSH / TELEGRAM-БОТОМ
========================================

Сделай хелпер:

sendFarmerSuggestion(farmer, suggestion) → void

Пока можно сделать заглушку, но интерфейс должен:

– получать Telegram id или способ связаться,
– отправлять текст сообщения.

Обновлять status в FarmerSuggestion:
– при успешной отправке → 'sent'.

========================================
6. API ДЛЯ АДМИНКИ (ОПЦИОНАЛЬНО)
========================================

Сделай endpoint только если это просто:

GET /api/farmer-suggestions
– фильтры: regionId, productKey, status
– для просмотра, что советуем фермерам.

========================================
7. ТРЕБОВАНИЯ К БЕЗОПАСНОСТИ И ПРОИЗВОДИТЕЛЬНОСТИ
========================================

– Все cron/job аккуратно ограничить по количеству записей (batch).
– Добавить индексы по:
  - SearchLog.regionId + createdAt,
  - FarmerDemand.regionId + productKey,
  - FarmerSuggestion.farmerId + productKey.

ИТОГ:
– Мы логируем спрос на фермерские товары,
– Вычисляем рост интереса по продуктам,
– И фермерам в этом регионе автоматически прилетают подсказки вроде:
  «Сегодня выгодно продавать картошку / малину / яйца», если реально есть спрос.

Реализуй описанную схему на backend, без изменений в существующих публичных API для обычных пользователей.
