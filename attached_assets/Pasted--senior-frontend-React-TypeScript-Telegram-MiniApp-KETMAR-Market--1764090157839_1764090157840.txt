Ты — senior frontend-разработчик (React + TypeScript, Telegram MiniApp) в проекте **KETMAR Market**.

Нужно ПОФИКСИТЬ и ПЕРЕРАБОТАТЬ фронт по следующим пунктам. Ниже — чёткое ТЗ, что именно сломано и как должно работать/выглядеть.

=================================
1. ГЛАВНАЯ СТРАНИЦА (HOME)
=================================

ПРОБЛЕМЫ:
- Сбился дизайн главного экрана.
- Карточки категорий стали квадратные и некрасивые, вместо стильных закруглённых.
- Вверху маленький логотип, много пустого белого места.
- Сейчас сверху показывается «Привет, Алексей!», но нет поисковой строки.

ЧТО СДЕЛАТЬ:

1. Вернуть современный дизайн карточек категорий:
   - Скругление углов карточек категорий: min 16–20px.
   - Тени мягкие, как у маркетплейсов (Avito/Яндекс).
   - Картинки категорий НЕ квадрат 1:1, а примерно 4:3 или 5:4, аккуратно вписанные внутрь.
   - Карточки должны снова выглядеть “premium”: как было ДО поломки.

2. Блок шапки (самый верх):
   - Убрать текущий маленький логотип + “Привет, Алексей!”.
   - Сделать ОДИН красивый блок:
     - Большой логотип **ketmar** по центру (ширина ~60% экрана, по высоте пропорционально).
     - Под логотипом — **поисковая строка** во всю ширину контейнера:
       - placeholder: `Что вы ищете?`
       - скруглённые углы (поле типа search), иконка лупы слева.
       - При нажатии Enter или по кнопке поиска:
         → автоматически переключать вкладку на **«Лента»**
         → запускать поиск по введённому запросу (радиус по умолчанию 3 км).

3. Убрать лишнее пустое белое пространство сверху:
   - Блок логотип + поиск должен быть компактным, не занимать пол-экрана.

Убедись, что главная страница снова выглядит:
- аккуратно,
- с закруглёнными карточками,
- без квадратных “уродливых” картинок.

=================================
2. ЛЕНТА И КАРТОЧКА ТОВАРА
=================================

Сейчас при переходе в ленту и при открытии объявления есть ряд проблем:

ПРОБЛЕМЫ:
- В карточке товара нет контактов продавца (телефон/username/Instagram).
- Блок “Похожие объявления”:
  - некликабельный,
  - криво выровнен,
  - карточки квадратные и смещены вправо.
- Основная карточка товара выглядит непрезентабельно.
- Кнопка «Назад» выглядит плохо и неудобна.
- Кнопка «Поделиться объявлением» не работает.

ЧТО СДЕЛАТЬ:

2.1. Карточка товара (детальный экран объявления)

Переделать экран объявления так:

1. Верхняя часть:
   - Большая галерея фото товара (carousel):
     - aspect-ratio 4:3 или 16:9, без чёрных полос.
     - Переключение между фото свайпом или стрелками.
     - Точки-индикаторы количества фото (● ○ ○).

2. Кнопка «Назад»:
   - Красивая круглая кнопка слева поверх фото:
     - фон: белый с лёгкой тенью,
     - внутренняя иконка стрелки влево (iOS-style),
     - при нажатии — возврат к предыдущему экрану (история навигации MiniApp).

3. Кнопка «Поделиться»:
   - Расположить справа сверху (рядом с избранным/сердечком).
   - При клике — вызывать `Telegram.WebApp.shareUrl(url)` или аналогичный механизм, чтобы поделиться ссылкой на объявление.
   - Убедиться, что урл объявления корректный и открывает это же объявление.

4. Основной блок с информацией:
   - Цена (крупно, жирно).
   - Локация + расстояние:
     - пример: `📍 Гомель • 483.1 км от вас`
   - Заголовок объявления.
   - Дата публикации + количество просмотров (например: `16.11.2025 • 1 просмотр`).

5. Контакты продавца:
   - Отдельный блок «Продавец»:
     - Имя или ник продавца.
     - Текст типа “На KETMAR с 2024 года” (если есть такие данные).
   - Ниже — блок контактов:
     - Кнопка **«Показать телефон»**, которая раскрывает телефон (берём из данных объявления/пользователя).
     - Если есть `username` Telegram → кнопка **«Написать в Telegram»** (deep-link `tg://resolve?domain=...`).
     - Если есть Instagram → маленькая кнопка/ссылка `Instagram`.

   Модель данных с backend предположительно уже содержит телефон / username / instagram
   (если нет — добавить поддержку получения этих полей из API объявления).

2.2. Похожие объявления

Проблема: карточки похожих объявлений сейчас:
- не кликабельные,
- выглядят квадратными,
- съехали вправо.

Сделать так:

1. Блок «Похожие объявления»:
   - Заголовок: “Похожие объявления”.
   - Ниже — горизонтальный скролл-карусель карточек (как лента товаров):
     - каждая карточка — кликабельна, открывает соответствующее объявление.
     - карточка с нормальным дизайном:
       - фото (4:3),
       - цена,
       - краткий заголовок,
       - город и расстояние (опционально).

2. Выравнивание:
   - карточки с одинаковой шириной,
   - одинаковые отступы слева/справа,
   - НИЧЕГО не должно уезжать вправо за пределы экрана.

Убедись, что block похожих объявлений:
- красиво скроллится,
- каждая карточка открывается по нажатию.

=================================
3. ТЕХНИЧЕСКИЕ МОМЕНТЫ / ОПТИМИЗАЦИЯ UI
=================================

1. Везде вернуть скруглённые углы там, где раньше были (карточки категорий, карточки объявлений, превью картинок и т.д.).
2. Проверить стили:
   - не ломать существующий дизайн других разделов,
   - использовать единую палитру (фирменный голубой, белый фон, тёмный текст).
3. Проверить адаптивность:
   - на маленьких экранах не должно быть перекосов и “наезда” карточек вправо.

=================================
4. ЧТО СДЕЛАТЬ В КОДЕ
=================================

1. Найти компонент главной страницы (Home).
   - Восстановить/переписать layout:
     - шапка (логотип + поиск),
     - сетка категорий с закруглёнными карточками и нормальными картинками.

2. Найти компонент детальной карточки объявления (AdDetails / ListingPage / ProductPage — в зависимости от названия).
   - Переделать:
     - галерею фото,
     - верхние кнопки (Назад, Поделиться, Избранное),
     - блок с ценой/городом/расстоянием/датой/просмотрами,
     - блок Продавец + контакты,
     - блок Похожие объявления (горизонтальная лента).

3. Убедиться, что:
   - телефон / username / instagram приходят из API и отображаются в UI;
   - при нажатии на похожие объявления происходит навигация к соответствующему объявлению;
   - кнопка «Поделиться» реально делится объявлением через Telegram WebApp.

4. После изменений:
   - пройти весь пользовательский сценарий:
     - зайти на Главную,
     - выбрать категорию,
     - открыть объявление,
     - посмотреть контакты,
     - нажать «Поделиться»,
     - пролистать похожие объявления,
     - вернуться «Назад».

Исправь текущие баги с дизайном, добейся аккуратного, современного и приятного UI, как у топовых маркетплейсов.