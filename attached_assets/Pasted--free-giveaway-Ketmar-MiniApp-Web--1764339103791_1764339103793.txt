Нужно дописать фичу категории «Даром отдаю» (free-giveaway) в Ketmar MiniApp/Web.

Смотри референс по дизайну в прикреплённых скриншотах (листинг в 2 колонки с плашкой «Даром» и синяя карточка снизу «Есть что отдать?»).

0. Контекст

Это та же категория, что у нас уже есть в дереве категорий: «Даром отдаю».

Важные требования:

Объявления из этой категории:

имеют цену = 0;

помечены как специальные: type = "giveaway" или флаг isFreeGiveaway = true;

НЕ попадают в обычные товарные категории (овощи, фрукты, фермерские и т.д.), даже если по смыслу похожи.

Подаваться такие объявления могут только из вкладки «Даром отдаю» (а не из обычной формы подачи).

В кабинете пользователя (Мои объявления) они отображаются отдельно помеченными (иконка подарка + шильдик «Даром»), но управляются так же: можно приостановить / удалить.

1. Экран категории «Даром отдаю» (листинг)

Сделай отдельный экран/route для категории Даром (если ещё нет):

Заголовок экрана:

Иконка подарка 🎁.

Текст: «Даром отдаю».

Подзаголовок: «X объявлений» (кол-во из API).

Под заголовком – инфо-баннер (розовый фон, закруглённые углы):

Иконка подарка.

Текст:
Здесь люди отдают вещи бесплатно. Заберите сегодня!

Листинг объявлений:

2-колоночная сетка карточек (как у нас для других категорий, но с особыми шильдиками).

На карточке:

Фотография (соотношение сторон как в основной ленте).

В левом верхнем углу поверх фото — яркий шильдик:

Фон розовый/малиновый (как на референсе),

Текст: «Даром» (или «Бесплатно» — но давай писать «Даром»).

В правом верхнем углу — кнопка «Избранное» (сердечко), если она уже есть в проекте.

Внизу карточки:

Название: title (обрезать по 2 строкам).

Строка с расстоянием: 📍 X км от вас.

Строка с датой публикации: 🕒 26 ноября, 14:30 (формат как в остальном приложении).

Если объявлений нет:

Показать пустой state:

Текст типа: «Пока никто ничего не отдает рядом…»

И сразу CTA-кнопку «Создать объявление» (см. синюю карточку ниже).

2. Синяя карточка снизу «Есть что отдать?»

Внизу экрана категории всегда должна быть закреплена (после списка объявлений) большая карточка-баннер:

Фон: синий (как на референсе).

Закруглённые края.

Содержимое по центру:

Иконка подарка (белая).

Текст:

Заголовок: «Есть что отдать?»

Подзаголовок: «Помогите другим и освободите место дома».

Кнопка:

Текст: «Создать объявление» (белая кнопка, синий текст).

Нажатие → открывает мастер создания объявления в категории «Даром отдаю» (см. раздел 3).

3. Форма подачи объявления «Даром отдаю»

Эти объявления подаются только из вкладки/экрана «Даром отдаю».
Нужно сделать отдельный сценарий подачи (можно переиспользовать общий мастер, но с особыми правилами).

3.1. Ограничения и бизнес-логика

Объявление типа «Даром»:

price = 0 (поле цены можно вообще не показывать пользователю).

Всегда привязано к ветке категорий только внутри дерева «Даром отдаю».

Не попадает в диффолтные фермерские/товарные категории.

В форме:

Ровно 1 фотография:

обязательная,

если пользователь пытается добавить больше — показать подсказку «В разделе “Даром” можно добавить только одну фотографию».

Короткое описание:

текстовое поле с лимитом символов (например 140–200),

под полем счётчик оставшихся символов.

Название товара:

обязательное поле,

используется как основной title в карточке.

Категория:

пользователь выбирает подкатегорию внутри раздела «Даром» (Одежда, Детские вещи, Мебель, Техника, Прочее и т.п.).

Можно сделать автоподбор подкатегории по названию/описанию, но объявление всё равно остаётся в ветке “Даром отдаю”, не попадает в овощи/фрукты/фермерские и т.д.

Локация:

используем тот же компонент гео, что и в обычной подаче (город, район, расстояние).

3.2. Структура мастера (минимум)

Шаг 1 – Основное:

Название.

Фотография (1 шт).

Шаг 2 – Описание:

Короткое описание.

Шаг 3 – Категория + место:

Выбор подкатегории внутри «Даром».

Автоопределение гео, город/район.

На последнем шаге кнопка «Опубликовать бесплатно».

После успешной публикации:

Показать экран/модалку:

«Объявление добавлено в раздел “Даром отдаю”».

Вернуть пользователя в экран категории «Даром отдаю», где его объявление уже видно в ленте.

4. Попадание объявлений «Даром» в кабинет пользователя

Все объявления, которые поданы в «Даром отдаю», должны отображаться у пользователя в Моих объявлениях / Мой товар:

В списке:

Отдельный визуальный маркер:

слева/сверху маленькая иконка подарка 🎁,

шильдик «Даром» (фон розовый, текст белый).

Цена не показывается (или явно написано «0 BYN» / «Бесплатно»).

Действия:

Кнопка/меню:

«Приостановить» / «Возобновить» (как у обычного объявления).

«Удалить».

Приостановленные объявления не должны показываться в ленте «Даром отдаю», пока снова не активированы.

В карточке детального просмотра объявления (если такая есть):

Отметка, что это раздел «Даром отдаю» (иконка подарка + текст «Даром»).

Цена/стоимость не отображать, вместо этого текст «Отдам бесплатно».

5. Фильтрация и подкатегории внутри «Даром отдаю»

Нужно обеспечить возможность фильтрации:

В верхней части экрана категории «Даром отдаю» сделай горизонтальный список/табы подкатегорий:

«Все»,

«Одежда»,

«Детские вещи»,

«Мебель»,

«Техника»,

«Прочее» (или какие подкатегории есть в нашем дереве).

Поиск:

строка поиска по названию/описанию,

работает только по объявлениям типа isFreeGiveaway = true.

6. Разграничение с другими категориями

Очень важно:

В общих лентах (фермерские товары, овощи, фрукты, обычные вещи) НЕ показывать объявления с type = "giveaway" / isFreeGiveaway = true.

Они должны быть видны:

только:

в ленте раздела «Даром отдаю»,

в "Моих объявлениях" владельца,

и, если нужно, в глобальном поиске только при включенном фильтре «Даром».

Проверь запросы к API:

Запрос на ленту «Даром» должен явно фильтровать по флагу isFreeGiveaway (или по категории типа DAROM_ROOT).

Запросы остальных лент должны исключать такие объявления.

7. Что нужно сделать по шагам

Найти/создать route/экран для категории Даром и привести его к описанному дизайну (шапка, баннер, сетка карточек, синяя карточка снизу).

Добавить/настроить модель объявления:

флаг isFreeGiveaway или type: "giveaway",

связь с веткой категорий Даром.

Сделать отдельный сценарий подачи объявления:

запуск из кнопки «Создать объявление» на экране «Даром отдаю»,

1 фото, короткое описание, название, выбор подкатегории в ветке «Даром»,

цена = 0, поле цены не показывать.

Обеспечить попадание таких объявлений в «Мои объявления»:

отдельный визуальный маркер (подарок + шильдик «Даром»),

действия «приостановить/удалить».

Настроить фильтры/поиск внутри раздела «Даром».

Убедиться, что объявления «Даром» не попадают в остальные категории/ленты, кроме:

раздела «Даром отдаю»,

кабинета автора.

Сделай так, чтобы весь UX был максимально похож на референс со скриншотов, но в стиле нашего приложения (цвета/радиусы/шрифты как у существующих карточек).