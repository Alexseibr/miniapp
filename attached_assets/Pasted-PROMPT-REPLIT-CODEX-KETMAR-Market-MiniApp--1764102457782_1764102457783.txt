PROMPT ДЛЯ REPLIT / CODEX
Задача: спроектировать и реализовать в KETMAR Market MiniApp
ЕДИНЫЙ МОДУЛЬ **«Умная фермерская платформа»**.

Она должна объединять:

1) Фермерский кабинет (отдельный раздел для продавцов-фермеров)
2) Специальные карточки фермерских товаров
3) Быструю массовую подачу (bulk create) фермерских товаров
4) Сезонную аналитику (цены, объёмы, спрос)
5) Аналитику карточек (показы, просмотры, клики по контактам)
6) Логирование поисковых запросов покупателей и «спрос рядом»
7) Умные уведомления фермерам (по спросу, цене, отсутствию предложений)
8) Рекомендации по ценам и действиям (поднять, снизить, допродать)

Работай поверх текущего стека проекта:

- Frontend: React + TypeScript, Telegram MiniApp (WebApp SDK)
- Backend: Node.js + TypeScript + Express/Nest-подобная архитектура
- БД: MongoDB (Mongoose)
- Уже есть:
  - модель объявления Ad (объявления),
  - пользователи User,
  - категории/подкатегории (есть категория «Фермерский рынок»),
  - геопоиск (GeoJSON + 2dsphere),
  - авторизация через Telegram initData (НЕ ломать),
  - лента объявлений и общий поиск.

=====================================================
0. ОБЩАЯ КОНЦЕПЦИЯ
=====================================================

**Цель:** сделать для фермеров отдельный «контур» в приложении,
который:

- помогает быстро закинуть сразу несколько товаров,
- показывает, как ведёт себя их товар по сезону и по району,
- показывает, что *сейчас* ищут покупатели рядом,
- сам подсказывает цену (ниже/выше рынка),
- шлёт уведомления: «в вашем районе ищут малину», «спрос вырос», «никто не смотрит».

Вся функциональность должна быть сгруппирована в разделе:

➡ **Фермерский кабинет**  
(отдельный экран/вкладка в MiniApp).

=====================================================
1. МОДЕЛИ ДАННЫХ — РАСШИРЕНИЕ ДЛЯ ФЕРМЕРОВ
=====================================================

1.1. Расширить модель `Ad` (объявление):

```ts
enum FarmerUnitType {
  KG = 'kg',
  HUNDRED_GRAMS = '100g',
  PIECE = 'piece',
  BUNDLE = 'bundle'
}

interface FarmerDetails {
  unitType: FarmerUnitType;  // кг, 100 г, штука, упаковка
  minQuantity?: number;      // минимальный заказ (например 0.5 кг)
  harvestDate?: Date;        // дата сбора (ягоды/овощи)
  productionDate?: Date;     // дата выпечки/изготовления
  isOrganic?: boolean;       // натуральный продукт / без химии
  stock?: number;            // остаток (в единицах unitType)
}

interface Ad {
  // ...
  categoryId: ObjectId;      // фермерский рынок или др.
  subcategoryId?: ObjectId;
  geo: {
    type: 'Point';
    coordinates: [number, number]; // [lng, lat]
  };

  farmerDetails?: FarmerDetails;

  // аналитика:
  impressionsTotal: number;
  impressionsToday: number;
  viewsTotal: number;
  firstShownAt?: Date | null;
  lastShownAt?: Date | null;
  contactClicks: number;
}
1.2. Модель для логов поиска и спроса:

ts
Копировать код
interface SearchLog {
  _id: ObjectId;
  userId?: ObjectId;
  sessionId?: string;
  query: string;
  normalizedQuery: string;
  detectedCategoryId?: ObjectId | null;
  lat?: number;
  lng?: number;
  radiusKm?: number;
  resultsCount: number;
  createdAt: Date;
}

interface SearchAlert {
  _id: ObjectId;
  userId?: ObjectId;
  sessionId?: string;
  normalizedQuery: string;
  detectedCategoryId?: ObjectId | null;
  lat?: number;
  lng?: number;
  radiusKm?: number;
  isActive: boolean;
  notifiedAt?: Date | null;
  createdAt: Date;
}
1.3. Модель агрегированного спроса (local demand):

ts
Копировать код
interface LocalDemandStats {
  _id: ObjectId;
  normalizedQuery: string;
  detectedCategoryId?: ObjectId | null;
  geoHash: string;         // район/город
  searchesCount: number;   // сколько раз искали за период
  period: 'hour' | 'day' | 'week';
  from: Date;
  to: Date;
  updatedAt: Date;
}
1.4. Модель сезонной аналитики (агрегированная статистика по фермерской категории):

ts
Копировать код
interface FarmerSeasonAnalytics {
  _id: ObjectId;
  categoryId: ObjectId;     // ягоды, овощи, выпечка и т.п.
  city: string;
  period: 'week' | 'month' | 'season' | 'year';
  from: Date;
  to: Date;

  totalAds: number;
  activeAds: number;

  averagePrice: number;
  minPrice: number;
  maxPrice: number;

  viewsTotal: number;
  contactClicksTotal: number;

  prevPeriodAveragePrice?: number;
  prevPeriodAds?: number;
  priceChangePercent?: number;
  adsChangePercent?: number;

  updatedAt: Date;
}
=====================================================
2. ФЕРМЕРСКИЙ КАБИНЕТ — СТРУКТУРА UI
Сделать отдельный экран FarmerDashboardScreen (Фермерский кабинет) с вкладками:

🟩 Мои товары

🧺 Быстрая подача

📈 Статистика

🔍 Спрос рядом

🔔 Уведомления (лог уведомлений/рекомендаций)

В нижнем меню MiniApp или в профиле добавить кнопку:
"Фермерский кабинет" (доступна только для пользователей с ролью фермер/продавец).

=====================================================
3. ВКЛАДКА «МОИ ТОВАРЫ»
Цель: показать все фермерские объявления пользователя с ключевыми метриками.

3.1. Backend:

GET /api/farmer/my-ads

Возвращает список Ad текущего пользователя, отфильтрованный по категориям фермерского рынка (и, возможно, связанным).

3.2. UI:

Для каждого объявления:

фото (или заглушка),

название,

цена + unitType:

пример: 12 BYN / кг,

свежесть:

«Собрано сегодня / вчера / X дней назад» (из harvestDate/productionDate),

статус:

активно / истекает через X дней / истекло / скрыто,

метрики:

👁 viewsTotal,

📢 impressionsTotal (можно компактно),

☎ contactClicks.

Опционально добавить цветовые индикаторы:

если просмотров мало → жёлтый/оранжевый,

если высокий CTR контакт-кликов → зелёный.

Кнопки действий:

Редактировать

Снять с публикации

Дублировать

=====================================================
4. ВКЛАДКА «БЫСТРАЯ ПОДАЧА» (массовая подача)
Экран FarmerBulkCreateScreen.

4.1. Frontend:

Список строк (по умолчанию 5):

Каждая строка включает:

Название товара

Подкатегория (select: Ягоды / Овощи / Фрукты / Выпечка / Молочка / Яйца / Мёд / Другое)

Цена (input number)

Единица (select: кг / 100 г / штука / упаковка)

Свежесть:

быстрые кнопки: Сегодня / Вчера / Указать дату

Остаток (stock)

Фото (кнопка «Фото» с превью, если выбрано)

Внизу:

+ Добавить ещё товар

Большая кнопка "Опубликовать все"

4.2. Backend:

Новый эндпоинт:

http
Копировать код
POST /api/farmer/bulk-ads
Body:

json
Копировать код
{
  "ads": [
    {
      "title": "Малина свежая",
      "subcategoryId": "...",
      "price": 12,
      "farmerDetails": {
        "unitType": "kg",
        "harvestDate": "2025-07-21T00:00:00.000Z",
        "stock": 10
      },
      "photos": ["url1", "url2"]
    },
    ...
  ]
}
Логика:

Игнорировать полностью пустые строки.

Для каждой строки:

создать Ad,

задать categoryId = «Фермерский рынок»,

подставить geo пользователя (из профиля или последней подачи),

сохранить farmerDetails.

Вернуть массив созданных объявлений.

=====================================================
5. КАРТОЧКА ФЕРМЕРСКОГО ТОВАРА (СПИСОК + ДЕТАЛЬНО)
5.1. В ленте / списках:

Создать компонент FarmerAdCard:

Фото (или иконка фермерского продукта).

Название (1–2 строки).

Крупная цена: 12 BYN / кг.

Мелко: 1.2 BYN / 100 г (если unitType = kg → автоматом считать цену за 100 г).

Свежесть:

бейдж:

«Сегодня» (зелёный),

«Вчера»,

«X дней назад» (серый/нейтральный).

Расстояние:

📍 1.2 км от вас.

5.2. Детальная карточка FarmerAdDetails:

Дополнительно к общему шаблону детальной карточки объявления:

Блок "Товар":

цена за unitType (крупно),

пересчёт в за 100 г/кг, если применимо,

минимальный заказ, пример:

«Минимальный заказ: 0.5 кг».

Блок "Свежесть":

«Собрано сегодня / вчера / X дней назад»

«Испечено сегодня» (если productionDate).

бейдж "Натуральный продукт" при isOrganic = true.

Блок "Расстояние":

📍 Брест, Гершоны • 1.2 км от вас.

Вставить аналитику:

строчка:

«Средняя цена по району: 11.5 BYN / кг»

«У вас: 12 BYN / кг»

«Цена выше рынка на 4%» / «Ниже рынка на 7%» / «Цена по рынку».

Данные для средней цены брать через эндпоинт сезонной аналитики (см. ниже).

=====================================================
6. АНАЛИТИКА КАРТОЧЕК (показы / просмотры / контакты)
6.1. Backend:

Эндпоинты:

http
Копировать код
POST /api/ads/:id/track-impression
POST /api/ads/:id/track-view
POST /api/ads/:id/track-contact
Логика:

track-impression:

impressionsTotal++

impressionsToday++

если firstShownAt пусто → now

lastShownAt = now

track-view:

viewsTotal++

track-contact:

contactClicks++

6.2. Frontend:

track-impression — вызывать при первом появлении карточки в viewport (IntersectionObserver).

track-view — при открытии детальной карточки.

track-contact — при клике на «Показать телефон / Написать в Telegram / Instagram».

В Фермерском кабинете, во вкладке "Мои товары", выводить эти цифры рядом с каждым объявлением.

=====================================================
7. СЕЗОННАЯ АНАЛИТИКА (ВКЛАДКА «СТАТИСТИКА»)
7.1. Backend:

Эндпоинт:

http
Копировать код
GET /api/farmer/season-analytics?city=&categoryId=&period=
city (или регион) — где считать.

categoryId/subcategoryId — ягоды/овощи/выпечка.

period: week / month / season / year.

Ответ:

json
Копировать код
{
  "categoryId": "...",
  "city": "Брест",
  "from": "2025-07-01",
  "to": "2025-07-31",
  "totalAds": 123,
  "activeAds": 45,
  "averagePrice": 12.5,
  "minPrice": 8,
  "maxPrice": 20,
  "viewsTotal": 2345,
  "contactClicksTotal": 320,
  "trend": {
    "prevPeriodAveragePrice": 11.0,
    "prevPeriodAds": 80,
    "priceChangePercent": 13.6,
    "adsChangePercent": 53.7
  }
}
7.2. UI — вкладка «Статистика»

Фильтры:

выбор категории (ягоды, овощи, выпечка, цветы, т.п.),

период: 7 дней / 30 дней / сезон / год.

Показывать:

Крупно: «Средняя цена: 12.5 BYN / кг»

под ним бейдж:

«+13% к прошлому периоду» или «−8% к прошлому месяцу».

Карточка:

«Объявлений за период: 123»

Снизу: «было 80 → +53%».

Карточка:

«Просмотры: 2345»

«Клики по контактам: 320»

«Конверсия: ~13.6%» (расчёт viewsTotal → contactClicks).

График (по возможности):

X — дни/недели,

Y — средняя цена или количество объявлений.

Рекомендации:

На основе средней цены по региону и цены конкретного товара/фермера выводить подсказки:

если выше рынка > X%:

«Цена выше средней по району на 18%. Можно снизить, чтобы продавать быстрее.»

если ниже рынка:

«У вас цена ниже рынка на 12% — это ваш плюс. Можно упаковать премиум-предложение.»

=====================================================
8. СПРОС РЯДОМ (ВКЛАДКА «СПРОС РЯДОМ»)
8.1. Backend:

Логирование поиска:

http
Копировать код
POST /api/analytics/search-log
Body:
{
  "query": "малина",
  "lat": 52.1,
  "lng": 23.7,
  "resultsCount": 0 или N
}
Сохранять SearchLog.

Регулярный worker (каждые 5–15 минут):

агрегирует SearchLog за последние 48 часов,

группирует по normalizedQuery, detectedCategoryId, geoHash,

считает searchesCount,

пишет/обновляет LocalDemandStats.

8.2. Эндпоинт спроса:

http
Копировать код
GET /api/farmer/local-demand?lat=&lng=&radiusKm=
Переводит lat/lng → geoHash и возвращает топ-запросы:

json
Копировать код
{
  "queries": [
    { "query": "малина", "searchesCount": 12 },
    { "query": "клубника", "searchesCount": 7 },
    { "query": "эклеры", "searchesCount": 4 }
  ]
}
8.3. UI:

Вкладка «Спрос рядом» показывает:

Заголовок: «Сейчас ищут рядом с вами»

Список:

«Малина — 12 запросов за 2 дня»

«Клубника — 7 запросов»

«Эклеры — 4 запроса»

Чипы-кнопки:

«Опубликовать малину»

«Опубликовать клубнику»
→ открывают массовую подачу/форму с заранее подставленным названием и категорией.

=====================================================
9. УМНЫЕ УВЕДОМЛЕНИЯ (ВКЛАДКА «УВЕДОМЛЕНИЯ» + TELEGRAM)
Нужен сервис FarmerNotificationService:

Функции:

notifyDemandSurge(farmerId, query, deltaPercent)

notifyMissingSupply(farmerId, query)

notifyGoodPrice(farmerId, productId, diffPercent)

notifyNoViews(farmerId, productId)

Логика:

Если спрос по запросу (например, «малина») вырос на X% за день:

находим фермеров, у которых:

уже есть малина,

или ранее были объявления категории «ягоды».

отправляем телеграм-уведомление:

«В вашем районе спрос на малину вырос на 80% за сутки!
Опубликовать свежую малину?»

Если люди часто искали, но объявлений нет:

«В радиусе 1 км искали эклеры, но предложений нет.
Хотите добавить свежую выпечку?»

Если цена фермерского товара сильно ниже рынка:

«У вас самая низкая цена на клубнику в районе — можно опубликовать больше, пока спрос высокий.»

Если товар не смотрят:

«Вашу картошку за 24 часа никто не смотрел.
Хотите изменить цену или обновить фото?»

Уведомления:

отправлять через существующий Telegram-бот (бот-backend),

дополнительно сохранять в коллекцию Notification для отображения во вкладке "Уведомления" фермерского кабинета.

=====================================================
10. ОБУЧАЕМЫЙ КЛАССИФИКАТОР КАТЕГОРИЙ
Нужен сервис:

http
Копировать код
POST /api/ai/classify-to-category
Body: { "title": "малина свежая" }
Логика:

использовать простой keyword-matching:

«малина», «клубника» → ягоды,

«картошка», «картофель» → овощи,

«хлеб», «пирожки», «эклеры» → выпечка,

«молоко», «сыр», «творог» → молочка,

и т.д.

словарь ключевых слов хранить в конфиге/коллекции, чтобы админ мог расширять.

возвращать:

detectedCategoryId,

detectedSubcategoryId.

При массовой подаче:

если фермер не выбрал категорию, попытаться определить её из title.

=====================================================
11. ОБЩИЕ ТРЕБОВАНИЯ
Везде использовать TypeScript.

Код структурировать по сервисам:

FarmerDashboardService,

FarmerBulkService,

FarmerAnalyticsService,

LocalDemandService,

FarmerNotificationService.

Ничего не ломать в существующей логике:

initData,

глобальная лента,

обычная подача объявлений.

Всё новое — по возможности опционально и включаемо флагами/ролями.

Дизайн: iOS-стиль,

мягкие скругления,

светлые карточки,

читаемые шрифты,

удобные кликабельные зоны (для фермеров и бабушек).

Сделай всю описанную функциональность в рамках текущего проекта
(backend + frontend), последовательно, с комментариями
в местах интеграции (где вызывать новые сервисы/эндпоинты).

yaml
Копировать код
