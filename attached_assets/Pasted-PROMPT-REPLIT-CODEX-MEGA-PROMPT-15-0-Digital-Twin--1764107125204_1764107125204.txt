PROMPT ДЛЯ REPLIT / CODEX

Задача:
Реализовать полный модуль 
🔥 MEGA-PROMPT 15.0 — «Digital Twin продавца»
= AI-ассистент продавца, работающий как «второй мозг».

Функции Digital Twin продавца:
- подсказывает какие товары продавать,
- прогнозирует спрос,
- говорит когда выкладывать,
- анализирует конкурентов,
- рекомендует цену,
- понимает качество фото и описаний,
- выявляет проблемы в магазине,
- даёт советы по выкладке,
- предупреждает когда товар "умирает",
- подталкивает к действиям (поднять цену, добавить фото),
- следит за трендами в районе,
- помогает фермерам (сезонность),
- автоматически обучается.

====================================================================
0. КОНЦЕПТ: DIGITAL TWIN ПРОДАВЦА
====================================================================

Digital Twin продавца = AI-модель, которая:
1) знает историю всех товаров продавца,
2) знает спрос вокруг,
3) знает конкурентов,
4) понимает качество объявлений продавца,
5) анализирует поведение покупателей,
6) строит прогнозы,
7) даёт советы в реальном времени.

Twin работает поверх:
- SellerProfile
- Ad
- AnalyticsEvent
- PriceEngine
- SeasonalInsight
- GeoEngine
- AI Engine (MEGA 12)
- Dynamic Price Engine (MEGA 13)
- Buyer Twin (MEGA 14)

====================================================================
1. BACKEND — МОДЕЛЬ DIGITAL TWIN ПРОДАВЦА
====================================================================

Создать:

`/backend/src/models/SellerTwin.ts`

```ts
interface SellerTwin {
  sellerId: ObjectId;

  // История товаров
  inventory: {
    adId: ObjectId;
    createdAt: Date;
    views: number;
    contacts: number;
    priceHistory: { price: number; date: Date }[];
    status: 'active' | 'inactive' | 'sold' | 'expired';
  }[];

  // Сегменты покупателей, интересующихся продавцом
  audience: {
    categoryId: ObjectId;
    demandScore: number;   // 0-1
    distanceAvg: number;   // среднее расстояние покупателей
  }[];

  // Проблемы
  issues: {
    type: 'bad_photos' | 'high_price' | 'low_price' | 'no_description' | 'low_demand' | 'wrong_category';
    adId: ObjectId;
    severity: number; // 0-1
    message: string;
  }[];

  // Рекомендации для продавца
  recommendations: {
    text: string;
    priority: 'low' | 'medium' | 'high';
    createdAt: Date;
  }[];

  // Предсказания
  predictions: {
    adId: ObjectId;
    expectedViews3d: number;
    expectedViews7d: number;
    chanceOfSale: number; // 0-1
  }[];

  // Фермерская сезонность
  seasonalInsights: {
    categoryId: ObjectId;
    nextPeakStart: Date;
    nextPeakEnd: Date;
    notes: string;
  }[];

  updatedAt: Date;
}
====================================================================
2. BACKEND — СЕРВИС SellerTwinEngine
Создать:

/backend/src/twin/SellerTwinEngine.ts

Функции:

2.1. trackSellerEvent(event)
Получает:

просмотры,

контакты,

избранное,

поисковые выдачи.

Обновляет inventory и audience.

2.2. analyzeListingQuality(ad)
Проверяет:

качество фото (AI Vision),

длину текста,

структуру,

корректность категории,

ключевые слова,

сезонность (фермерские товары),

цену относительно рынка.

Заполняет issues.

2.3. getRecommendations(sellerId)
Генерирует советы:

Примеры:

«У товара X нет фото — добавьте 2–3 фото»

«Цена завышена на 18% по сравнению с рынком»

«Спрос на ягоды вырос — добавьте свежую партию»

«Лучшее время публикации для хлеба — 07:00–10:00»

«У конкурентов мало объявлений в 1 км — поднимите цену»

«Добавьте тег “мёд свежий” — его часто ищут»

2.4. predictOutcome(ad)
AI предсказывает:

шанс продажи,

прогноз просмотров,

оптимальное время публикации.

2.5. findMissedOpportunities(sellerId)
Twin ищет:

категории, которые продавец не использует, но там спрос,

потенциальные товары (особенно фермерские),

время когда спрос выше.

Пример:

«В радиусе 3 км ищут клубнику → добавьте в ассортимент».

2.6. monitorMarketChanges()
Раз в 10 мин проверяет:

цены конкурентов,

количество объявлений в категории,

сезонность,

всплески спроса,

пустые ниши.

И создаёт советы продавцу.

====================================================================
3. BACKEND — API
Создать контроллер:

/api/seller-twin/*

3.1. Получить всю аналитику Twin
GET /api/seller-twin/overview

Возвращает:

quality report,

issues,

советы,

предсказания,

сезонность,

missed opportunities,

прогноз спроса.

3.2. Получить рекомендации
GET /api/seller-twin/recommendations

3.3. Получить проблемы
GET /api/seller-twin/issues

3.4. Получить прогнозы по товарам
GET /api/seller-twin/predictions

====================================================================
4. FRONTEND MINIAPP — UI ТWИНА ПРОДАВЦА
Экран:

SellerTwinScreen.tsx

Разделы:

📊 ОБЗОР (Dashboard Seller Twin)
просмотры за неделю,

открытие контактов,

рынок около продавца,

hottest categories рядом,

динамика спроса.

🔥 РЕКОМЕНДАЦИИ AI
«Добавьте фото товару #123»

«Цена выше рынка на 18%»

«Выгодное время публикации: 7:00–10:00»

«Категория “ягоды” в пике — актуализируйте товары»

⚠ ПРОБЛЕМЫ
Список issues:

плохие фото,

пустое описание,

неверная категория,

слишком высокая/низкая цена.

📈 ПРОГНОЗЫ И ШАНС ПРОДАЖИ
Карточки:

Товар:

шанс продажи 74%

просмотры в следующие 3 дня: 210

рекомендуемая цена: 320 руб

🍓 ФЕРМЕРСКИЙ БЛОК (если isFarmer)
когда начинается сезон конкретного товара,

когда пик спроса,

что люди ищут рядом,

график сезонности.

💡 УПУЩЕННЫЕ ВОЗМОЖНОСТИ (Missed Opportunities)
Примеры:

«Ищут домашнее молоко — у вас такого нет»

«5 запросов на выпечку сегодня утром»

«3 человека искали “клубника свежая” в 1 км»

====================================================================
5. УВЕДОМЛЕНИЯ SELLER TWIN
Digital Twin продавца отправляет пуши:

«Спрос в вашей категории вырос на 27%»

«Появились новые конкуренты»

«Ваш товар X выглядит хуже других — добавьте фото»

«Время пиковой активности покупателей — сейчас!»

«Цены на рынке выросли — можно поднять свою»

====================================================================
6. AI ВЗАИМОДЕЙСТВИЕ
Twin использует:

AiEngine (описания, теги, фотокачество)

PriceEngine (рекомендуемая цена)

SeasonalInsight (сезонность)

GeoEngine (спрос в районе)

HotspotEngine (холодные/горячие зоны)

====================================================================
7. НЕ ЛОМАЕМ ТЕКУЩИЙ КОД
Требования:

не менять модели Ad, User, SellerProfile

SellerTwin — отдельный слой

Twin не блокирует публикацию объявления

Twin работает в фоне

Все аналитические данные — read-only для остальных модулей

====================================================================

Реализовать всё на TypeScript.