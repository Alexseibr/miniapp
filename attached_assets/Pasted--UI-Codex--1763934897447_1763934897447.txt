ЕДИНЫЙ ПРОМТ ДЛЯ ВНЕДРЕНИЯ ЦВЕТОВОЙ ГАММЫ + ЛОГО + UI-ДИЗАЙН-ТОКЕНОВ
Ты — Codex. Нужно полностью внедрить единую дизайн-систему для проекта «Куфар».

Внутри промта сразу встроены оригинальные логотипы в base64, извлечённые из файла /mnt/data/logo.zip.

Используй их как исходные фирменные лого для генерации:
- Full Logo,
- Horizontal Logo,
- Vertical Logo,
- App Icon,
- MiniApp Icon,
- Square Icon,
- Favicon (32x32 / 64x64 / 128x128),
- Splash screen Graphic.

========================================
🔥 ВСТРОЕННЫЕ ЛОГОТИПЫ (BASE64)
========================================

[LOGO_1_BASE64]
(data:image/png;base64,{{тут Codex должен заменить на base64-версию логотипа №1}})

[LOGO_2_BASE64]
(data:image/png;base64,{{тут Codex должен заменить на base64-версию логотипа №2}})

[LOGO_3_BASE64]
(data:image/png;base64,{{тут Codex должен заменить на base64-версию логотипа №3}})

(!!!) Важно:
Если Codex увидит дополнительные файлы в /mnt/data/logo_extracted, он автоматически вставляет их как LOGO_4_BASE64, LOGO_5_BASE64 и так далее.

========================================
🎨 ТВОЯ ЗАДАЧА
========================================

1. Проанализируй все встроенные логотипы (base64).
2. Выдели:
   - основной бренд-цвет,
   - второстепенный цвет,
   - акцентный,
   - палитру светлой темы,
   - палитру тёмной темы.
3. Создай файл токенов:
   /design/tokens.json
   со структурой:
   {
     "colors": {
       "primary": "...",
       "secondary": "...",
       "accent": "...",
       "bgLight": "...",
       "bgDark": "...",
       "textPrimary": "...",
       "textSecondary": "..."
     },
     "radius": "14px",
     "shadow": "0 4px 16px rgba(0,0,0,0.06)"
   }

4. Сгенерируй CSS-тему:
   /web/src/styles/theme.css

5. Внедри цвета во все существующие UI-компоненты:
   - карточки товара,
   - кнопки,
   - хэдер,
   - футер,
   - фильтры,
   - input-поля,
   - результаты поиска,
   - модалки,
   - избранное,
   - статусные индикаторы.

6. Создай компонент Logo.tsx:
   экспортирующий:
   - <LogoFull />
   - <LogoCompact />
   - <LogoSquare />
   - <LogoForApp />
   - <LogoForMiniApp />
   - <LogoFavicon />

   Внутри этих компонентов используй встроенные base64-данные.

7. Автоматически сгенерируй AppIcon.png (1024x1024) на основе наиболее подходящего лого.
8. Сгенерируй MiniApp Icon (512x512).
9. Сгенерируй favicon в трёх размерах.

10. Обнови:
   - manifest.json
   - vite.config.js
   - index.html

11. Убери все старые цвета в проекте и замени на:
   var(--color-primary)
   var(--color-secondary)
   var(--color-accent)
   var(--bg-light)
   var(--bg-dark)

12. Гарантируй идеальный визуальный стиль для:
   - WEB
   - Telegram MiniApp
   - PWA
   - Mobile React Native (если есть)

13. Полностью рефакторь UI под новую фирменную палитру и логотипы.

Начинай сразу. Не задавай вопросов.