ромпт для Codex (Replit)
Хочу, чтобы ты продолжил разработку проекта маркетплейса объявлений (аналог Kufar), который уже частично реализован у меня в репозитории.

ТЕХНОЛОГИИ:
- Node.js + Express (backend API)
- MongoDB + Mongoose (модели)
- Telegraf (один общий Telegram-бот)
- В дальнейшем: MiniApp (Telegram WebApp) и web-витрина, которые ходят к этому же API

СУТЬ ПРОЕКТА:
- Это доска объявлений, похожая на Kufar.by:
  - категории/подкатегории,
  - поиск объявлений,
  - карточка объявления.
- Но есть важное отличие:
  - Kufar показывает город/область,
  - А я хочу делать акцент на **расстоянии от покупателя**.
- У нас **ОДИН общий Telegram-бот**, без отдельных нишевых ботов.
  - Ниши, ярмарки, ремесленники — это просто категории/сезоны внутри одного бота и одного backend.

УЖЕ СДЕЛАНО (исходя из текущего кода):
- Перевод backend, запуска бота и загрузки БД на CommonJS.
- Настроена централизованная конфигурация среды (dotenv, config).
- Реализованы Mongoose-модели:
  - User, Category, Season, Ad, Order.
- Настроены Express-маршруты:
  - `GET /api/categories` — дерево категорий,
  - `GET /api/seasons` — список сезонов,
  - `GET /api/ads` / `GET /api/ads/:id` / `POST /api/ads`,
  - базовый `/health` и корневой `/` (API работает...).

ТЕПЕРЬ НУЖНО СДЕЛАТЬ СЛЕДУЮЩЕЕ (ФОКУС НА ГЕО И ВЫДАЧЕ):

🧩 ОБЩАЯ ИДЕЯ
Сделать так, чтобы при поиске объявлений (и в API, и дальше в MiniApp/web):

1. Мы могли передавать координаты покупателя (`lat`, `lng`) в запрос.
2. Backend считал расстояние от покупателя до каждого объявления.
3. В ответе API на каждое объявление возвращались:
   - город продавца (строкой),
   - расстояние от покупателя (в метрах/километрах).
4. На фронте (позже) это будет отображаться как, например:
   - `Минск • 850 м от вас`
   - `Брест • 12.3 км от вас`
5. **Без точного адреса и улицы**, только город и расстояние.

⚙️ ЗАДАЧИ ДЛЯ BACKEND (СДЕЛАЙ ПОШАГОВО):

1) Расширить модель Ad и/или User для хранения города
   - В модели объявления `Ad` добавь поле:
     - `city: String` (город продавца или места услуги/товара).
   - Если в модели `User` уже есть или логичнее хранить `city`, можешь:
     - или продублировать `city` в `Ad`,
     - или при выдаче объявления подтягивать город из `User`.
   - Для простоты и производительности лучше сделать так:
     - в `Ad` хранить `city` как поле строки.

2) Убедиться, что у объявления есть координаты
   - В `Ad` уже есть `location` (lat, lng).
   - Предполагаем, что для объявлений с геопоиском (`realty`, `country_base`, `farm`, `craft` и т.п.) `location` заполняется.
   - Если нет — просто не считаем расстояние (или считаем расстояние = null).

3) Добавить helper-функцию для расчёта расстояния
   - Создай утилиту, например, `utils/distance.js` (или любой другой файл), с функцией типа:

     ```js
     function haversineDistance(lat1, lng1, lat2, lng2) {
       // возвращает расстояние в метрах или километрах
     }
     ```

   - Можно использовать стандартную формулу haversine.
   - Важно: не используй внешние тяжёлые библиотеки, достаточно собственной функции.

4) Доработать endpoint `GET /api/ads`
   - Добавь поддержку query-параметров:
     - `lat` — широта покупателя (число),
     - `lng` — долгота покупателя (число),
     - опционально: `maxDistanceKm` — максимальный радиус (например, для фильтра “рядом со мной”).
   - Логика:
     - Если `lat` и `lng` не переданы:
       - работает как сейчас — просто выдаёт объявления по категориям/фильтрам.
     - Если `lat` и `lng` переданы:
       - На уровне MongoDB можно оставить обычный find (без GeoJSON на этом этапе),
       - В приложении, после выборки объявлений, пройти по ним и для каждого:
         - если у объявления есть `location`, рассчитать расстояние до покупателя,
         - добавить к объекту объявления поле, например:
           - `distanceMeters` (Number),
           - и, возможно, `distanceKm` (Number, округлённый до одного знака).
       - Если указан `maxDistanceKm`, отфильтровать объявления, у которых `distanceKm <= maxDistanceKm`.
       - Можно добавить сортировку:
         - сначала по расстоянию (если есть), потом по дате.

   - В ответе `GET /api/ads` каждое объявление должно содержать:
     - `_id`, `title`, `price`, `currency`, `city`,
     - `distanceMeters`/`distanceKm` (если координаты покупателя были переданы и были координаты у объявления).

5) Продумать формат расстояния (для фронта)
   - Для фронтового UI будет удобно, если backend вернёт готовые данные:
     - `distanceMeters` (для точной логики),
     - но фронт сам решает, как форматировать (м или км).
   - Либо можно сразу вернуть человеко-читаемую строку:
     - `distanceLabel: "850 м"`, `distanceLabel: "12.3 км"`.
   - На этом этапе можно оставить просто `distanceMeters`, а форматирование сделать позже на фронте.

6) Сделать черновой endpoint для “рядом со мной”
   - Можно добавить endpoint:
     - `GET /api/ads/nearby?lat=...&lng=...&maxDistanceKm=...&categoryId=...`
   - Внутри этот endpoint может переиспользовать логику `GET /api/ads`, но с обязательными `lat/lng` и `maxDistanceKm`.
   - Это пригодится:
     - для квартир в аренду,
     - для “базы отдыха до 50 км от меня”,
     - для ремесленников/фермеров рядом.

7) Учесть один общий бот
   - У меня **один общий бот**, а не много нишевых.
   - Архитектуру бота и API нужно строить так, чтобы:
     - бот просто передавал нужные фильтры в API,
     - нишевость (фермеры, ремесленники, авто, аренда, ярмарки) определялась:
       - по категориям (`Category.slug`),
       - по сезону (`seasonCode`),
     - и в WebApp/MiniApp мы могли открывать:
       - общую витрину,
       - витрину конкретной ниши,
       - витрину конкретного сезона.
   - Но **самих отдельных ботов** (например `FermerBot`, `CraftBot`) не будет — всё через одного бота и один MiniApp, просто с разными параметрами.

ПРОСЬБА К ТЕБЕ (CODEx):

1. Пройди по этим шагам последовательно.
2. Сначала покажи изменения в моделях (Ad, возможно User).
3. Затем реализуй helper для расстояния.
4. Потом доработай `GET /api/ads` (и, по желанию, отдельный `/api/ads/nearby`).
5. В конце покажи пример JSON-ответа `GET /api/ads` с полями `city` и `distanceMeters`, чтобы было понятно, что будет отдавать API.

Формат ответа:
- Пожалуйста, давай полный код файлов, которые меняешь (с путями), и кратко поясняй, что 