PROMPT ДЛЯ REPLIT / CODEX

Задача: создать полноценный модуль 
🔥 MEGA-ANALYTICS 10.0 — «Аналитика продавца PRO»  
уровня Avito Pro / Yandex Market / Etsy Pro.

Модуль должен работать поверх существующей архитектуры:
- User / SellerProfile / Ad
- GeoEngine / HotspotEngine / SeasonalInsight
- NotificationEngine
- транзакционные действия (просмотры, клики, контакты)
- AI слой (AiGateway)

====================================================================
0. ЦЕЛЬ МОДУЛЯ
====================================================================

Сделать аналитику продавца, которая показывает:

1) просмотры товаров продавца,  
2) сколько раз открыли контакты,  
3) карта спроса вокруг продавца,  
4) тепловая карта интереса по его категориям,  
5) сравнение цены с рынком,  
6) график продаж/просмотров по времени,  
7) всплески спроса рядом,  
8) AI-советы продавцу, что продавать,  
9) подписчики продавца,  
10) рейтинг продавца и отзывы.

====================================================================
1. BACKEND — СОБЫТИЯ ДЛЯ АНАЛИТИКИ
====================================================================

Создать модель:

```ts
AnalyticsEvent {
  userId?: ObjectId;           // кто сделал действие
  sellerId: ObjectId;          // к какому продавцу относится
  adId?: ObjectId;             // если к товару
  type: 'view' | 'contact' | 'favorite' | 'search_hit' | 'price_view'
  geo?: { lat, lng }           // точка события
  createdAt: Date;
}
Фиксировать:

каждый просмотр товара,

каждый клик на телефон/Telegram,

каждое добавление в избранное,

попадание товара в поисковую выдачу,

просмотр карточки магазина.

====================================================================
2. BACKEND — СЕРВИС АНАЛИТИКИ SELLER ANALYTICS ENGINE
Создать модуль:

/backend/src/analytics/SellerAnalyticsEngine.ts

Функции:

2.1. Просмотры по каждому товару
ts
Копировать код
getViewsTimeline(sellerId, period)
2.2. Просмотры магазина
ts
Копировать код
getStoreViews(sellerId, period)
2.3. Клиki на контакты
ts
Копировать код
getContactClicks(sellerId, period)
2.4. Аналитика по категориям
ts
Копировать код
getCategoryPerformance(sellerId)
Возвращает:

популярность категорий,

какие товары чаще смотрят,

какие категории продавца проваливаются.

2.5. Сравнение цены с рынком (AI + статистика)
ts
Копировать код
getPricePosition(sellerId)
Возвращает список товаров:

ниже рынка на X%

выше рынка на Y%

best price (самый дешёвый в районе)

2.6. Карта спроса по товарам продавца (heatmap)
ts
Копировать код
getDemandHeatmap(sellerId, radiusKm)
Строится на основе событий:

search_hit,

empty_search,

proximity search,

просмотры соседних похожих товаров.

2.7. Гео-тепловая карта интереса
ts
Копировать код
getSellerHotspots(sellerId)
Выводит точки на карте, где:

чаще всего смотрят товары данного продавца,

чаще всего ищут похожие товары.

2.8. Подписчики
ts
Копировать код
getSubscribersStats(sellerId)
динамика подписок,

новые подписчики,

отписки.

2.9. AI советы продавцу
ts
Копировать код
getAISuggestions(sellerId)
Советы:

«В районе вырос спрос на ___, добавьте товар»

«Ваш товар ___ стоит дороже рынка на 23%»

«Покупатели часто ищут ___, но у вас нет этого товара»

«Вы продаёте дешевле рынка, можно поднять цену на 12%»

====================================================================
3. BACKEND — API АНАЛИТИКИ
Создать маршруты:

GET /api/seller-analytics/overview?sellerId=...&period=7d

GET /api/seller-analytics/views?sellerId=...

GET /api/seller-analytics/contacts?sellerId=...

GET /api/seller-analytics/category-performance?sellerId=...

GET /api/seller-analytics/price-position?sellerId=...

GET /api/seller-analytics/demand-heatmap?sellerId=...

GET /api/seller-analytics/hotspots?sellerId=...

GET /api/seller-analytics/subscribers?sellerId=...

GET /api/seller-analytics/suggestions?sellerId=...

Все методы должны читать req.currentUser и проверять:

продавец ↔ данные только свои,

админ ↔ доступ ко всем.

====================================================================
4. FRONTEND MINIAPP — ПРОФЕССИОНАЛЬНЫЙ КАБИНЕТ ПРОДАВЦА (UX)
Экран:
/miniapp/src/screens/SellerAnalyticsScreen.tsx

Общий стиль — как «Avito Pro / Yandex Market Seller».

Блоки:

4.1. Обзор (Dashboard)
просмотры товаров за неделю,

открытие контактов,

подписчики,

top-3 товара по просмотрам,

предупреждения:

«4 товара не имеют фото»

«2 товара стоят выше рынка»

«Спрос на категорию Овощи вырос на 18%»

4.2. Геокарта спроса
Карта + heatmap:

где ищут товары продавца,

где чаще всего смотрят.

4.3. График просмотров (line chart)
просмотры по дням,

клики в контакты.

4.4. Категории продавца
Таблица:

категория,

просмотров,

средняя цена,

цена продавца,

рекомендация AI.

4.5. Аналитика цен
Для каждого товара:

цена продавца,

средняя по рынку,

минимальная в районе,

рекомендация: «оптимальная цена — …»

4.6. Подписчики
Блок:

подписчиков за неделю,

новые подписчики,

отписки.

4.7. Сезонные рекомендации (для фермеров)
AI блок:

«Пик спроса на клубнику начинается через 10 дней»

«Выпечка лучше продаётся утром (6–10 утра)»

====================================================================
5. PUSH-ТРИГГЕРЫ ДЛЯ ПРОДАВЦА
Триггеры:

вырос спрос на его категорию,

покупатели ищут товар, которого у продавца нет,

цена конкурентов снизилась,

всплеск просмотров,

сезонный пик.

Примеры:

«⚡ В вашем районе ищут: пылесосы. Добавьте товар?»

«📉 Товар Apple iPhone 12 стал дороже рынка на 18%»

«🍓 Спрос на ягоды вырос на 22%»

====================================================================
6. ПАНЕЛЬ АДМИНА (Расширение)
В /admin/seller-analytics добавить:

список продавцов,

просмотры,

теплые зоны,

карты спроса,

подозрительные пики активности (анти-накрутка),

анализ сезонности по продавцу.

====================================================================
7. НЕ ЛОМАЕМ СУЩЕСТВУЮЩИЙ КОД
Ad остаются без изменений,

SellerProfile — подключаем аналитику к sellerId,

ничего не трогаем в гео, только используем существующие данные,

единая архитектура MiniApp/WebView (PlatformAdapter).

====================================================================

Выполнить всё на TypeScript + Node + React MiniApp.

Выполнять постепенно, аккуратно, с комментариями.