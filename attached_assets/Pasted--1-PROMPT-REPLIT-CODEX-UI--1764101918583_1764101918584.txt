🥕 Промпт №1 — Карточка фермерского товара
PROMPT ДЛЯ REPLIT / CODEX
Задача: сделать отдельный UI и логику для **карточки фермерского товара** в KETMAR Market MiniApp.

Цель: фермерские товары (ягоды, овощи, выпечка, молочка и т.п.) должны отображаться по-особенному:
- иконка/фото,
- цена "за 1 кг" и "за 100 г" (или за штуку),
- свежесть (сегодня / вчера / несколько дней),
- расстояние до продавца.

=====================================================
1. КОНТЕКСТ
=====================================================

Стек:
- Frontend: React + TypeScript (Telegram MiniApp).
- Backend: Node.js + TypeScript + MongoDB (Mongoose).

В БД уже есть модель объявления Ad, в ней:
- title, description, price, categoryId, subcategoryId,
- geo (Point [lng, lat]),
- createdAt / updatedAt и т.п.

У нас есть отдельная **категория "Фермерский рынок"** и её подкатегории (овощи, фрукты, ягоды, выпечка, молочка и т.д.).

=====================================================
2. БЭКЕНД — ДОПОЛНИТЕЛЬНЫЕ ПОЛЯ ДЛЯ ФЕРМЕРСКИХ ОБЪЯВЛЕНИЙ
=====================================================

Нужно расширить модель объявления (Ad):

```ts
interface FarmerDetails {
  unitType: 'kg' | '100g' | 'piece' | 'bundle';  // единица измерения
  minQuantity?: number;      // минимальный заказ (например 0.5 кг)
  harvestDate?: Date;        // дата сбора (для ягод/овощей)
  productionDate?: Date;     // дата выпечки/изготовления
  isOrganic?: boolean;       // можно указать "натуральное / без химии"
}

interface Ad {
  // ...
  farmerDetails?: FarmerDetails;
}


Правила:

unitType обязателен для объявлений из категории "Фермерский рынок".

harvestDate или productionDate — опционально, но если указано → используем для "свежести".

=====================================================
3. ФРОНТ — КОМПОНЕНТ КАРТОЧКИ ФЕРМЕРСКОГО ТОВАРА

Создать отдельный компонент, например:
FarmerAdCard (для ленты / списка)
и FarmerAdDetails (для детальной карточки).

3.1. В списке (лента / "Популярное рядом")

Карточка должна показывать:

Фото товара (если нет → заглушка с иконкой овощей/ягод).

Название (1–2 строки).

Цена:

крупно: 12 BYN / кг

мелко под ней: 1.20 BYN за 100 г (если unitType = 'kg', автоматически считать).

Статус свежести:

если сегодня harvestDate/productionDate → бейдж "Сегодня" (зелёный).

если вчера → "Вчера".

если > 2–3 дней → показывать "X дней назад" более нейтрально.

Расстояние до продавца:

📍 1.2 км от вас.

3.2. Детальная карточка (FarmerAdDetails)

Дополнительно к обычной карточке объявления:

Блок "Товар":

название,

цена за кг / за 100 г / за штуку,

минимальный заказ (например: "Минимальный заказ: 0.5 кг").

Блок "Свежесть":

текст:

"Собрано сегодня" / "Собрано вчера" / "Собрано X дней назад",

или "Испечено сегодня/вчера/...".

если есть isOrganic → бейдж "Натуральный продукт".

Блок "Расстояние":

как и для обычных объявлений, но выделить:

📍 Брест, Гершоны • 1.2 км от вас.

=====================================================
4. ЛОГИКА ВЫБОРА ФЕРМЕРСКОЙ КАРТОЧКИ

В любом списке объявлений:

если categoryId относится к "Фермерский рынок" → использовать FarmerAdCard,

иначе — обычную карточку.

В детальной странице:

если фермерская категория → встраивать FarmerAdDetails.

=====================================================
5. МЕЛОЧИ

Учти iOS-стиль: мягкие скругления, приятные тени, аккуратные бейджи.

Не ломай текущий функционал избранного, гео, аналитики карточки.

Если нет harvestDate/productionDate — просто не показывать "свежесть", без ошибок.

Реализуй:

расширение модели Ad,

рендер фермерских карточек,

переключение типов карточек в ленте и в деталке.


---

## 🧺 Промпт №2 — **Быстрая массовая подача для фермеров**

```text
PROMPT ДЛЯ REPLIT / CODEX
Задача: реализовать **быструю массовую подачу фермерских товаров** в KETMAR Market MiniApp.

Идея: фермер заходит и сразу заполняет список из 5–10 позиций (картошка, морковь, малина, клубника и т.п.) в одном экране, без отдельного мастера «одно объявление = 3 шага».

=====================================================
1. КОНТЕКСТ
=====================================================

Есть стандартный мастер подачи объявления (гео → фото → описание → цена и т.п.).

Нужно сделать **отдельный режим**:

- "Фермерская подача",
- доступный:
  - либо через кнопку в категории "Фермерский рынок",
  - либо из профиля фермера,
  - либо по спец-линку из бота.

=====================================================
2. ФРОНТ — ЭКРАН МАССОВОЙ ПОДАЧИ
=====================================================

Создать экран (компонент) `FarmerBulkCreateScreen`.

Верхняя часть:

- заголовок: **"Быстрая подача для фермеров"**
- подсказка: "Добавьте сразу несколько товаров — ягоды, овощи, выпечку."

Ниже — **табличка / список** из N строк (например, 5 по умолчанию, с возможностью добавлять новые):

Каждая строка = один товар:

Поля:
1. Название товара (input).
   - подсказка: "Малина, картошка, хлеб, пирожки..."

2. Подкатегория (select):
   - Ягоды / Овощи / Фрукты / Выпечка / Молочка / Яйца / Мёд / Другое.

3. Цена:
   - число + select единицы:
     - BYN за 1 кг / за 100 г / за штуку / за упаковку.

4. Свежесть:
   - date-picker или быстрый выбор:
     - "Сегодня" / "Вчера" / "Указать дату".

5. Количество/остаток (опционально):
   - "Сколько есть в наличии" (например, 10 кг, 20 шт).

6. Фото:
   - маленькая кнопка "Фото".
   - нажали → открывается выбор изображения.
   - Если фермер не хочет добавлять фото, разрешаем подать без фото.

Внизу каждой строки:
- кнопка "удалить строку" (крестик), если строк больше 1.

Внизу экрана:
- кнопка "Добавить ещё товар" (добавляет пустую строку).
- большая кнопка "Опубликовать все" (primary).

=====================================================
3. БЭКЕНД — МАССОВОЕ СОХРАНЕНИЕ
=====================================================

Сделать новый эндпоинт:

```http
POST /api/farmer/bulk-ads


Body (пример):

{
  "ads": [
    {
      "title": "Малина свежая",
      "categoryId": "...(фермерский рынок)...",
      "subcategoryId": "...(ягоды)...",
      "price": 12,
      "farmerDetails": {
        "unitType": "kg",
        "harvestDate": "2025-07-21T00:00:00.000Z"
      },
      "stock": 10,
      "photos": ["url1", "url2"]
    },
    {
      "title": "Картофель молодой",
      "subcategoryId": "...(овощи)...",
      "price": 1.5,
      "farmerDetails": {
        "unitType": "kg",
        "harvestDate": "2025-07-20T00:00:00.000Z"
      },
      "stock": 50
    }
  ]
}


Логика:

Для каждого элемента:

создаём отдельный документ Ad,

проставляем:

категорию "Фермерский рынок",

geo продавца (как в обычной подаче),

farmerDetails, цену и т.д.

Возвращаем список созданных объявлений/их ID.

Обязательно:

валидация на бэке:

пропускаем только заполненные строки (где есть хотя бы title + price),

игнорируем пустые.

=====================================================
4. ИНТЕГРАЦИЯ С УЖЕ ИМЕЮЩЕЙСЯ ГЕО/ПРОФИЛЕМ

При массовой подаче не спрашиваем заново геолокацию, а берём:

текущую точку пользователя (как в обычной подаче),

либо его сохранённый адрес/район.

Продавец (user) один и тот же для всех созданных объявлений.

=====================================================
5. UI/UX МЕЛОЧИ

При успешной массовой подаче:

показываем экран-результат:

"Создано X объявлений",

список с короткими карточками и кнопкой "Посмотреть в Мои объявления".

Если ни одной строки не заполнено → показывать ошибку "Добавьте хотя бы один товар".

Реализуй:

новый экран,

форму с несколькими строками,

фото (допуская массовый аплоад по 1 на товар),

бэкенд-эндпоинт,

интеграцию в меню (кнопка "Быстрая подача для фермеров").


---

## 📈 Промпт №3 — **Сезонная аналитика для фермерских товаров**

```text
PROMPT ДЛЯ REPLIT / CODEX
Задача: реализовать **сезонную аналитику** по фермерским товарам в KETMAR Market.

Примеры:
- сколько продаётся тюльпанов в марте,
- сколько картошки и малины,
- как меняется средняя цена по рынку,
- показывать эти данные фермеру в простом виде.

=====================================================
1. КОНТЕКСТ
=====================================================

Есть:
- объявления категории "Фермерский рынок" (ягоды, овощи, выпечка и т.п.),
- у каждого объявления:
  - категория/подкатегория,
  - цена,
  - createdAt,
  - farmerDetails.harvestDate/productionDate (может, станет),
  - geo (город/район),
  - аналитика (impressions/views/contactClicks — уже внедряем).

Нужно считать статистику:
- по периодам (неделя, месяц, сезон),
- по категориям/подкатегориям в конкретном городе/районе.

=====================================================
2. БЭКЕНД — АГРЕГАЦИЯ СЕЗОННОЙ АНАЛИТИКИ
=====================================================

Создать сервис/эндпоинт:

```http
GET /api/farmer/season-analytics?city=Брест&categoryId=...&period=month


Параметры:

city или region — где считаем (можно по geo-ключу),

categoryId или subcategoryId — что считаем (ягоды, картофель, тюльпаны),

period:

week, month, season, year,

или указать конкретные даты from / to.

Результат должен содержать:

{
  "categoryId": "...",
  "city": "Брест",
  "from": "2025-03-01",
  "to": "2025-03-31",
  "totalAds": 123,                    // сколько объявлений было создано
  "activeAds": 45,                    // сколько активных
  "averagePrice": 12.5,               // средняя цена
  "minPrice": 8,
  "maxPrice": 20,
  "viewsTotal": 2345,                 // суммарные просмотры
  "contactClicksTotal": 320,          // клики по контактам
  "trend": {
    "prevPeriodAveragePrice": 11.0,   // средняя цена за прошлый период
    "priceChangePercent": +13.6,      // %, вверх/вниз
    "prevPeriodAds": 80,              // было объявлений
    "adsChangePercent": +53.7
  }
}


Агрегация:

Использовать Mongo aggregation pipeline:

фильтр по categoryId/subcategoryId,

фильтр по geo (город/район),

фильтр по дате (createdAt / harvestDate / productionDate — что более уместно),

$group по периоду,

расчёт avg/min/max цены,

суммарные viewsTotal и contactClicks.

=====================================================
3. ФРОНТ — ЭКРАН "Сезонная аналитика"

Сделать отдельный экран (например, в профиле фермера):
FarmerAnalyticsScreen.

Содержимое:

Фильтры сверху:

выбор категории:

ягоды / овощи / фрукты / выпечка / цветы / другое,

выбор периода:

"7 дней", "30 дней", "Сезон", "Год".

выбор гео:

по умолчанию — текущий город/район фермера.

Основной блок:

карточка:

"Средняя цена" — крупное число (например, 12.5 BYN / кг),

под ним:

бейдж типа:

"ниже прошлогодней на 8%" или

"выше, чем месяц назад на 12%".

карточка:

"Объявлений за период" — 123,

под ним: "было 80 → +53%".

"Просмотры и интерес":

"Просмотры: 2 345",

"Контакты: 320",

"Конверсия: 13.6%".

График (по возможности):

простая линия/бар по дням/неделям:

по оси X — время,

по оси Y — средняя цена или количество объявлений.

Подсказки фермеру:

текстовый блок:

если цена фермера сильно выше средней → подсказка:
"Средняя цена в вашем районе за этот товар: 12.5 BYN.
У вас: 18 BYN. Возможно, стоит скорректировать цену."

если ниже → "У вас цена ниже средней на 20% — это может ускорить продажи."

=====================================================
4. ИНТЕГРАЦИЯ С ПОДАЧЕЙ И КАРТОЧКОЙ

Позже (можно предусмотреть):

при подаче фермерского объявления отображать небольшую подсказку:

"Средняя цена за последние 7 дней: 12.5 BYN / кг."

на карточке товара показывать бейдж:

"Ниже рынка на 10%" / "Цена в рынке" / "Выше рынка на 15%."

Сейчас главное — сделать backend-аналитику и базовый экран FarmerAnalyticsScreen с выводом данных.

=====================================================
5. ОБЩИЕ ТРЕБОВАНИЯ

TypeScript везде.

Не ломать существующие модели и логику (расширяем аккуратно).

Использовать уже существующую структуру категорий и гео.

Всё, что делаешь для фермерской аналитики, стараться делать в виде переиспользуемых сервисов (можно будет применять к недвижимости, авто и т.д.).

Реализуй:

backend-агрегацию сезонной аналитики,

новый эндпоинт /api/farmer/season-analytics,

экран FarmerAnalyticsScreen,

базовую визуализацию (без супер-сложных графиков, но структурированно и понятно).