ПРОМТ ДЛЯ REPLIT: ГЛАВНАЯ ЭКРАН KETMAR + БЛИЖАЙШИЕ ОБЪЯВЛЕНИЯ

Ты — senior фронтенд/фуллстек разработчик.
Нужно допилить главный экран Telegram MiniApp KETMAR Market.

Сейчас у меня главная выглядит так:

Сверху: заголовок KETMAR Market + подпись «мини-приложение».

Ниже: приветствие «Привет, Алексей!».

Потом строка поиска.

Потом сетка категорий.

Я хочу изменить главный экран, чтобы он выглядел и работал так:

Шапка как в нативном приложении:

Сверху крупным текстом бренд: KETMAR (без слова Market и без подписи «мини-приложение»).

Справа — иконка уведомлений (колокольчик) с маленьким красным индикатором, если есть новые события (можно пока сделать просто заглушку / клик по ней ничего не делает).

Внизу под шапкой — строка поиска, визуально как в iOS:

placeholder: Найти товары, продукты, услуги...

слева иконка лупы.

поле должно быть кликабельным всегда (sticky).

Блок “Быстрые категории” под поиском:

Заголовок: Быстрые категории.

Сетка категорий в виде карточек с закруглёнными углами.

Примеры быстрых категорий (можешь привязать к нашим реальным категориям/подкатегориям):

Фермерский рынок

Овощи/фрукты

Выпечка свежая

Личные вещи

Одежда

Обувь

Бытовые мелочи

Телефоны & Электроника

Сельхоз техника

Услуги

Аренда инструмента

Даром отдаю

При клике на быструю категорию:

происходит переход/фильтрация к списку объявлений по этой категории (либо отдельный экран, либо скролл к ленте с предустановленным фильтром — как удобнее, но чтобы работало).

Блок “Рядом с вами” как на третьем скрине:

Ниже быстрых категорий мне нужен блок:

Заголовок: Рядом с вами

Подзаголовок меньшим шрифтом: В радиусе X км
(где X — динамический радиус, см. ниже логику геопоиска).

Справа от подзаголовка — кнопка-чип 🗺 Карта:

по нажатию открывается экран карты с объявлениями (если у нас уже есть такая карта — подключи её; если нет, сделай переход-заглушку с TODO).

Под этим — горизонтальный или вертикальный список карточек объявлений, как в примере:

Превью-фото

Бейджик “Свежее” / статус (можно использовать уже существующее поле, если есть)

Цена (₽ / BYN — как в проекте)

Название / короткое описание

Карточка кликабельна → открывает страницу объявления.

Поведение при скролле / “потянуть вверх”:

Когда пользователь на главной тянет экран вверх / скроллит вниз:

после блока “Быстрые категории” должна начинаться лента ближайших объявлений из блока “Рядом с вами”.

Строка поиска должна оставаться всегда доступной и кликабельной:

сделай её position: sticky под системной шапкой MiniApp (или внутри собственного контейнера),

чтобы при прокрутке вниз строка поиска закреплялась сверху экрана, а под ней скроллилась лента категорий и объявлений.

То есть UX такой:

Открываем главную → видим шапку KETMAR + поиск + быстрые категории.

Начинаем тянуть вверх → быстрые категории уезжают вверх, под ними появляются карточки объявлений “Рядом с вами”.

Строка поиска всё время «прибита» в верхней части экрана MiniApp.

Логика геопоиска “Рядом с вами”:

В проекте уже должна быть либо есть, либо ты можешь добавить поддержку геопоиска через backend. Если нужно — используй/добавь endpoint вида:

GET /api/ads/nearby?lat=...&lng=...&radiusKm=...

с фильтрами по категориям при необходимости.

Задача по логике:

При открытии главной:

Определяем геолокацию пользователя (через Telegram WebApp location, браузерную navigator.geolocation или уже существующий механизм в проекте).

Дальше запускаем алгоритм подбора радиуса:

Стартовый радиус: 0.3 км (300 метров).

Делаем запрос /api/ads/nearby.

Если объявлений достаточно мало (например, меньше 8–10 штук), увеличиваем радиус ступенями:

0.3 км → 1 км → 3 км → 5 км → 10 км (значения можешь вынести в константу массива RADIUS_STEPS).

На каждой ступени:

делаем запрос на backend,

накапливаем / обновляем список объявлений.

Цель: получить видимое количество объявлений (например, >= 10), но при этом не уходить в слишком большой радиус.

Нужно предусмотреть максимальный радиус, после которого мы просто показываем, что получилось (например, 20 км).

В UI:

в подзаголовке В радиусе X км отображаем фактический радиус, на котором было найдено нужное количество объявлений (или последний использованный).

если объявлений нет вообще даже на максимальном радиусе:

показываем заглушку:
Рядом с вами пока нет объявлений. Попробуйте изменить категорию или добавить своё объявление.

но блок “Рядом с вами” всё равно должен корректно отображаться.

Все ошибки геолокации и запросов:

не должны ломать главную.

если гео не доступно → можно:

показать блок “Рядом с вами” со статусом
Не удалось определить местоположение + кнопку “Повторить попытку”.

или fallback на популярные объявления без гео.

Техническая реализация (frontend):

Используем React (и текущий стек мини-приложения).

Желательно вынести логику главного экрана в отдельный компонент, например HomeScreen / HomePage.

Состояния (примерно):

userLocation (lat, lng, status: loading / ok / error),

nearbyAds (список, статус загрузки),

currentRadiusKm,

isLoadingMoreRadius,

searchQuery.

Для строки поиска:

отдельный компонент SearchBar со sticky позиционированием.

по вводу/submit должен вызывать уже существующий поиск (если он есть) или просто логировать TODO.

Верстка:

адаптирована под узкий мобильный экран (Telegram MiniApp),

большие отступы, скругленные карточки, как в примере нативных приложений.

не ломать текущий нижний таб-бар (Главная / Лента / Мои / Избранное / Профиль или аналогичный).

Backend-часть (если нужно доработать):

Если в проекте нет нормального эндпоинта /api/ads/nearby:

Реализуй его в Node.js + MongoDB (скорее всего у нас уже есть модель Ad):

Поле geo в объявлении:
geo: { type: "Point", coordinates: [lng, lat] }

2dsphere-индекс по geo.

Запрос по радиусу с фильтрацией по статусу (активные объявления).

Обрати внимание на порядок координат: [lng, lat].

Верни данные, которые нужны для карточки:

id, title, price, currency, thumbnailUrl, badges/labels, distance до пользователя (можно посчитать через $geoNear и вернуть distanceMeters).

Требования к результату:

В итоге после твоих правок должно быть:

Обновлённый главный экран:

шапка KETMAR + иконка уведомлений,

строка поиска (sticky),

блок “Быстрые категории”,

блок “Рядом с вами” с заголовком, радиусом, кнопкой “Карта” и карточками объявлений.

При скролле вверх:

появляется лента ближайших объявлений,

строка поиска всегда доступна и кликабельна.

Геопоиск:

старт с 300 м,

динамическое расширение радиуса до нахождения минимального количества объявлений или достижения максимального радиуса,

корректная обработка ошибок геолокации.

Код аккуратный:

без лишних console.log в прод-зоне,

с понятными комментариями в ключевых местах,

без поломки существующих экранов MiniApp.

Если нужно будет что-то задокументировать — добавь короткое описание поведения главного экрана и геопоиска в README.