PROMPT ДЛЯ REPLIT / CODEX  
Задача: полностью переработать ГЛАВНЫЙ ЭКРАН MiniApp KETMAR Market по варианту №2  
(гео + поиск + “популярное рядом” + компактные категории).

=====================================================
0. КОНТЕКСТ ПРОЕКТА
=====================================================

Стек:

- Frontend: React + TypeScript, Telegram MiniApp (WebApp SDK).
- Backend: Node.js + TypeScript, Express, MongoDB (Mongoose).
- У нас уже есть:
  - гео-поиск объявлений (`/api/ads/nearby`, `/api/ads/search`),
  - категории/подкатегории,
  - вкладки внизу: Главная / Лента / Мои / Избранное / Профиль,
  - авторизация через initData (НЕ ломать!).

Сейчас главная страница = большая сетка категорий.  
Нужно изменить её логика и UI.

=====================================================
1. ГЛАВНЫЙ ПРИНЦИП НОВОЙ ГЛАВНОЙ
=====================================================

Главная страница должна быть:

- завязана на гео: “что продают рядом со мной”,
- иметь сверху компактный блок с местоположением,
- иметь строку поиска,
- показывать:
  - “Популярное в вашем районе” (объявления),
  - немного основных категорий (4–6 штук),
  - кнопку “Все категории” → отдельный экран со старой сеткой.

Полная сетка категорий НЕ должна больше занимать весь первый экран.

=====================================================
2. ОНБОРДИНГ ГЕОПОЗИЦИИ (ПЕРВЫЙ ЗАПУСК)
=====================================================

Если у пользователя ещё нет сохранённого гео (нет lat/lng в локальном состоянии/хранилище):

1) При первом заходе в MiniApp показываем ОТДЕЛЬНЫЙ экран/слой-онбординг:

   - Заголовок:  
     **"Покажем, что продают рядом с вами"**
   - Подзаголовок:  
     "Определим район, чтобы показывать объявления около вас."
   - Кнопки:
     - Большая primary:
       **"Определить автоматически"**  
       → вызывает `Telegram.WebApp.requestLocation()` или браузерный geolocation API (в проекте уже есть гео-инфраструктура — используй её).
     - Вторая кнопка (secondary):
       **"Выбрать на карте"**  
       → если в проекте уже есть карта — используй её. Если нет — пока можешь сделать заглушку (TODO), но структуру оставь.

2) После успешного получения lat/lng:

   - сохраняем координаты в локальном сторе (контекст / Redux / Zustand — что используется),
   - закрываем онбординг,
   - открываем **главный экран** (описан ниже).

3) При последующих открытиях MiniApp, если lat/lng уже известны:

   - онбординг НЕ показывать,
   - сразу открывать основной главный экран.

=====================================================
3. НОВЫЙ ГЛАВНЫЙ ЭКРАН — СТРУКТУРА
=====================================================

Главный экран (та вкладка, которая сейчас “Главная”) должен выглядеть так:

СВЕРХУ:

1) Маленький/аккуратный логотип KETMAR (уже есть — можно использовать текущий компонент).

2) Блок местоположения и радиуса:

   - строка вида:
     `📍 Брест, Гершоны • 3 км`
   - это один кликабельный чип / кнопка.
   - по нажатию открывается модалка/экран настройки:
     - смена точки (автоопределение / выбор на карте),
     - настройка радиуса (например: 1 км, 3 км, 5 км, 10 км → можно сделать чипами/слайдером).
   - значение по умолчанию радиуса = 3 км (если другое — ориентируйся на существующие настройки проекта).

3) СТРОКА ПОИСКА:

   - placeholder: **"Что вы ищете?"**
   - иконка лупы слева.
   - стиль iOS: скруглённый прямоугольник, светлый фон.
   - ЛОГИКА:
     - пользователь вводит текст → нажимает Enter/поиск на клавиатуре.
     - при этом:
       1) мы переключаемся на вкладку **"Лента"**,
       2) выполняем поиск объявлений ВОКРУГ ТЕКУЩЕГО МЕСТОПОЛОЖЕНИЯ (используем lat/lng и radius),
       3) используя новый/существующий API (см. секцию 4).

НИЖЕ:

4) Блок **"Популярное в вашем районе"**:

   - Заголовок: **"Популярное рядом"**.
   - Подзаголовок поменьше:  
     "Свежие объявления около вас".
   - Под ним 3–6 карточек объявлений (по вертикали или каруселью).
   - Данные брать из `/api/ads/nearby`:
     - сортировка по времени создания (самые свежие),
     - ограничить по радиусу (текущий radius),
     - limit = 5–6 штук.

   Если объявлений нет → показывать пустое состояние:
   - текст: "Пока нет объявлений рядом. Попробуйте увеличить радиус или выбрать категорию."

5) Блок **"Популярные категории"**:

   - Заголовок: **"Популярные категории"**.
   - Ниже горизонтальная лента/строка чипов или 2 ряда крупных карточек, НО БЕЗ огромной сетки на весь экран.
   - Показываем 4–6 основных категорий, например:
     - Фермерский рынок
     - Недвижимость
     - Авто и запчасти
     - Услуги
     - Ремонт и стройка
     - Путешествия
   - Источник категорий:
     - можно захардкодить список ID/slug в фронте,
     - либо сделать API `/api/categories/popular` (опционально).
   - ЛОГИКА клика по категории:
     - НЕ делать галочку,
     - переход на экран **полного дерева категорий/подкатегорий**, уже отфильтрованный по этой категории,
     - либо сразу на Ленту с фильтром по категории (реши, что лучше исходя из текущей архитектуры: можно сделать так — по клику → экран подкатегорий).

6) Кнопка/строка **"Все категории"**:

   - отдельная большая кнопка/строка ниже популярных категорий:
     - текст: "Все категории"
     - иконка стрелки/chevron вправо.
   - По нажатию открывает **экран "Все категории"**:

     - на этом экране можно показывать ТЕКУЩУЮ сетку категорий (красивая, iOS-стиль, 2 или 3 колонки),
     - при клике по категории здесь — поведение как мы ранее описывали:
       - либо экран подкатегорий,
       - либо сразу Лента с фильтром по категории.

=====================================================
4. ПОИСК — СВЯЗКА ГЛАВНАЯ ↔ ЛЕНТА
=====================================================

Важно: поиск на главной НЕ должен сам по себе рисовать список объявлений.  
Вся выдача должна жить во вкладке **"Лента"**.

### 4.1. API

Нужен/используется эндпоинт:

```http
GET /api/ads/search?q=&lat=&lng=&radiusKm=&categoryId=&subcategoryId=


q — поисковая строка.

lat/lng — текущая геолокация пользователя.

radiusKm — радиус (с блока местоположения).

categoryId/subcategoryId — опционально, если мы идём в Ленту из конкретной категории.

Если в проекте уже есть /api/ads/search — расширить его до такой сигнатуры и добавить геофильтрацию вокруг точки.

4.2. Фронт: поведение поля поиска на главной

При Enter/submit:

Берём текущие lat/lng и radiusKm из гео-контекста.

Навигируем пользователя на вкладку "Лента" с параметрами:

query = q,

lat/lng,

radiusKm.

На экране Ленты:

при инициализации проверяем:

если пришёл query → делаем запрос /api/ads/search.

если пришла только гео-инфа без query → показываем геоленту (/api/ads/nearby).

Важно:

поиск на главной и поиск на Ленте должны пользоваться ОДНОЙ логикой и одним API, чтобы поведение было одинаковым.

=====================================================
5. КАТЕГОРИИ: ПОВЕДЕНИЕ КЛИКА

Сейчас на главной по клику по категории:

появляется синяя рамка и галочка,

категорию "выделяет", но не происходит переход.

Нужно ИЗМЕНИТЬ:

На НОВОЙ главной:

карточка категории (в блоке "Популярные категории"):

НЕ выделяется галочкой,

при клике:

либо открывает экран подкатегорий,

либо сразу открывает Ленту с фильтром по этой категории и текущим georadius.

На экране "Все категории":

каждую карточку категории сделать кликабельной с тем же поведением.

НИКАКИХ чекбоксов/галочек на карточке категорий быть не должно.

Если в коде был state selectedCategory именно для галочки на главной — удали/переделай.

=====================================================
6. ВИЗУАЛ И МЕЛОЧИ

Верхний блок (логотип + гео + поиск) должен занимать МАЛО места по высоте, чтобы уже на первом экране (без скролла) пользователь видел:

гео,

поиск,

хотя бы 1–2 объявления в блоке "Популярное рядом".

Стили:

Использовать уже принятый iOS-стиль:

светлый фон,

плавные тени,

скругления карточек 18–24px,

красивые иконки категорий (3D картинки, которые уже есть).

Если геопозиция ещё не задана, а мы почему-то оказались на главной:

в блоке гео показывать:

📍 Местоположение не задано

кнопку "Определить".

при нажатии — запускать тот же процесс, что и в онбординге.

Учти, что проект — Telegram MiniApp:

не ломай инициализацию WebApp,

не ломай нижнюю навигацию.

=====================================================
7. ЧТО НЕ ТРОГАТЬ

Авторизацию через initData.

Бэкенд-модели объявлений и категорий (кроме расширения search при необходимости).

Экран подачи объявления, Лента, Избранное, Профиль — кроме изменяемых точек интеграции поиска/категорий (как описано выше).

=====================================================
8. ИТОГ

Нужно:

Реализовать онбординг геолокации (первый запуск).

Сделать НОВЫЙ главный экран:

верхний блок: гео + поиск,

блок "Популярное рядом" (объявления вокруг пользователя),

блок "Популярные категории" (4–6 штук),

кнопку "Все категории" → отдельный экран сетки категорий.

Исправить логики:

поиск на главной → Лента + /api/ads/search с georadius,

клик по категории → подкатегории или Лента с фильтром,

убрать галочку выделения категорий на главной.

Сохранить iOS-стиль и учесть все edge cases (нет гео, нет объявлений, нет результатов поиска).

Сделай все эти изменения в коде проекта (frontend + при необходимости backend),
аккуратно и последовательно.