Промпт №1 — “Живые категории” (динамический рост/сжатие подгрупп)
Ты — senior backend+frontend разработчик KETMAR Market.

Нужно реализовать систему **«живых категорий»**:

- подкатегории и под-подкатегории появляются, когда объявлений достаточно,
- и автоматически «сворачиваются» обратно в родительскую категорию, когда объявлений мало,
- учитываем ещё и географию (город/район).

================================
1. БИЗНЕС-ЛОГИКА
================================

Идея:

1) У каждой категории/подкатегории есть счётчик объявлений:
   - глобальный (по всей стране),
   - локальный (по городу / району).

2) Правила появления подкатегории в UI:
   - Если в подкатегории >= N объявлений в данном городе/районе
     → показываем подкатегорию отдельно.
   - Если объявлений < N
     → не показываем эту подкатегорию в фильтрах / дереве,
       а показываем объявления как часть родительской категории.

3) Пример:
   - Категория: "Техника"
   - Подкатегория: "Телефоны" — появляется, если в городе >= 5 объявлений.
   - Подкатегория: "Айфоны" — появляется, если в городе >= 3 айфонов.
   - Если меньше → ищем их через "Техника" → "Телефоны", без доп. ветки "Айфоны".

4) Для просмотра "по всей стране":
   - можно использовать другой порог (например, N_country),
   - либо вообще показывать всё дерево полностью.

================================
2. БЭКЕНД
================================

Сделай:

1) В Mongo схему Category с полями:
   - type: 'category' | 'subcategory' | 'subsubcategory'
   - parentId?: ObjectId
   - slug, title
   - thresholds:
     - minAdsLocal (например, 3)
     - minAdsCountry (например, 10)

2) Сервис, который считает количество объявлений по категориям:
   - `CategoryStats` с полями:
     - categoryId
     - geoKey (например, "city:Брест" или "region:Брестский")
     - adsCount
     - updatedAt

   - Раз в X минут/часов рассчитываем аггрегацией:
     - по активным объявлениям,
     - учитывая их geo (город/район).

3) API-метод:
   - `GET /api/categories/tree?geo=...&scope=local|country`
   - возвращает дерево категорий, в котором подкатегории фильтруются по правилам:
     - если `adsCount < minAdsLocal` (для scope=local) → НЕ включать в список, но объявления остаются в родителе.
     - то же самое для scope=country с `minAdsCountry`.

4) Важно:
   - родительская категория ВСЕГДА видна;
   - если у родителя все подкатегории скрыты (мало объявлений),
     то просто показываем родителя без раскрытия дерева.

================================
3. ФРОНТЕНД (MiniApp)
================================

1) Используй новый API `/api/categories/tree` вместо жёстко забитого списка.
2) На главной странице и в фильтрах:
   - показывай только те подкатегории, которые пришли с бэка.
   - если у категории есть подкатегории — отображай стрелку/chevron для раскрытия.
   - если нет — сразу идём в ленту с фильтром по этой категории.

3) Для случая "все объявления по стране":
   - используем scope=country,
   - дерево будет полнее.

================================
4. ДОПОЛНИТЕЛЬНО
================================

1) Параметризуй пороги:
   - глобально в конфиге (`MIN_ADS_LOCAL`, `MIN_ADS_COUNTRY`)
   - или на уровне категорий.

2) Обязательно:
   - не ломай существующую подачу объявлений (на подачу можно показывать полное дерево, чтобы продавец мог выбрать точную подкатегорию),
   - а вот в поиске/фильтрах для покупателя показывать только «живые» ветки.

Реализуй все описанные схемы, сервисы, агрегации и API.

🎨 Промпт №2 — Полный UI Kit в стиле iOS для MiniApp
Ты — lead UI/Frontend engineer. Нужно создать **UI Kit в стиле iOS** для KETMAR Market MiniApp (React + TypeScript).

Цель: 
- унифицировать внешний вид,
- сделать все элементы похожими на нативные компоненты iOS.

================================
1. БАЗОВЫЕ ПРИНЦИПЫ
================================

1) Цвета:
   - primary: #2A82F0 (голубой)
   - primarySoft: #E6F0FF
   - textMain: #111111
   - textSecondary: #6B7280
   - background: #F6F7FB
   - surface: #FFFFFF
   - danger: #EF4444
   - success: #16A34A

2) Скругления:
   - большие карточки: 18–24px
   - кнопки: 12–16px
   - бейджи: круг/пилюля

3) Тени:
   - мягкие, размытые (как на iOS), без резких границ.

4) Шрифты:
   - семантические размеры: title, subtitle, body, caption и т.п.
   - например:
     - h1: 22–24, bold
     - h2: 18–20, semibold
     - body: 14–16, regular
     - caption: 12–13, regular/medium

================================
2. КОМПОНЕНТЫ UI KIT
================================

Создай отдельную директорию `ui/` и в ней:

1) **Button**
   - варианты: primary, secondary, ghost, danger
   - состояния: default, hover, pressed, disabled
   - с иконкой слева/справа.

2) **IconButton**
   - круглая кнопка с иконкой (например, назад, избранное, share)
   - версии: light (на белом фоне), dark (на фото).

3) **Card**
   - базовый контейнер с тенью и скруглением.
   - можно передавать variant: flat | elevated.

4) **Badge**
   - маленькие бейджи:
     - числовой (подкатегории, количество)
     - статусный (новое, скоро закончится и т.п.)

5) **Input / SearchInput**
   - обычный текстовый инпут
   - search-инпут в стиле iOS:
     - иконка лупы, placeholder, скругление.

6) **Chip / FilterChip**
   - маленькие чипы-фильтры (радиусы, категории, теги).
   - состояния: selected / unselected.

7) **BottomNavBar**
   - единый компонент нижнего меню:
     - иконка + подпись
     - выбранный таб подсвечивается «таблеткой» или подчеркиванием.

8) **Modal / BottomSheet**
   - модальное окно
   - bottom sheet в стиле iOS:
     - с хэндлом сверху, скруглением 24px.

9) **ListItem**
   - элемент списка:
     - иконка/аватар слева,
     - заголовок, подзаголовок,
     - chevron справа.

10) **Toast / Snackbar**
   - краткие уведомления (успех, ошибка).

================================
3. РЕАЛИЗАЦИЯ
================================

1) Все компоненты должны быть:
   - на TypeScript,
   - с понятными пропсами и дефолтами,
   - переиспользуемыми.

2) Вынеси:
   - константы цветов,
   - размеры,
   - скругления
   в отдельный файл `ui/theme.ts`.

3) Пример использования:
   - обнови главную страницу и карточку товара так, чтобы они использовали новые компоненты UI Kit, а не «разрозненные стили».

================================
4. ДОПОЛНИТЕЛЬНО
================================

1) Добавь простые анимации (fade, slide) при появлении карточек.
2) Убедись, что компоненты хорошо смотрятся на разных размерах экрана.
3) Подготовь короткий README с примерами.

Сделай полный UI Kit, как описано выше, и внедри его хотя бы в пару ключевых экранов (Главная, Лента, Карточка товара).

🃏 Промпт №3 — Redesign карточки товара «как Avito Premium»
Ты — senior frontend designer/developer. Нужно полностью переработать карточку товара в KETMAR Market MiniApp в стиле **“Avito Premium / современный маркетплейс”**.

Карточка = экран просмотра одного объявления (детальная страница).

================================
1. ЦЕЛЬ ДИЗАЙНА
================================

Сделать так, чтобы:

- карточку было приятно листать,
- фото и цена — главные,
- контакт продавца — максимально понятный,
- расстояние и гео — явно видны,
- похожие объявления — аккуратная карусель.

Ориентир: Avito Premium, auto.ru, современные маркетплейсы.

================================
2. ВЕРХНИЙ БЛОК (ФОТО + КНОПКИ)
================================

1) Галерея фото:
   - полноэкранная ширина,
   - aspect-ratio 4:3 или 16:9,
   - свайп влево/вправо,
   - индикатор страниц (точки) по центру снизу.

2) Кнопка «Назад»:
   - круглая, белая, с тенью, в левом верхнем углу поверх фото.
   - по нажатию — возврат к предыдущему экрану.

3) Кнопка «Поделиться» и «Избранное»:
   - в правом верхнем углу,
   - такие же круглые белые кнопки с иконками.
   - "Поделиться" должна вызывать Telegram share (через WebApp API).

================================
3. БЛОК ЦЕНЫ И ОСНОВНОЙ ИНФО
================================

Под фото — «premium-card»:

- крупная цена (самый большой текст на экране).
- под ней:
  - `📍 Город • X км от вас`
- ниже — заголовок объявления (2–3 строки, обрезка многоточием).

Сделай этот блок на белой карточке с закруглением 24px и тенью,
 слегка отделённой от фона.

================================
4. ВРЕМЯ И СТАТИСТИКА
================================

Отдельная строчка:

- `📅 16.11.2025  •  👁 21 просмотр  •  ❤ 5 в избранном`

Все данные брать из модели (просмотры/лайки, если их ещё нет — добавить поля на бэке, но UI подготовить).

================================
5. БЛОК «ПРОДАВЕЦ»
================================

Отдельная карточка:

- слева круглый аватар с буквами (инициалы продавца),
- справа:
  - имя (ник) крупным,
  - текст меньшим шрифтом: «На KETMAR с 2024 года», «X активных объявлений».

Под этим:

- кнопки:
  - **Написать в Telegram** (если есть username),
  - **Показать телефон** (раскрывает номер),
  - опционально: Instagram.

Кнопки стилизовать в один ряд или в две строки, в стиле iOS:

- primary (Написать)
- secondary (Показать телефон)

================================
6. ОПИСАНИЕ + ХАРАКТЕРИСТИКИ
================================

Секция:

- заголовок «Описание»
- текст описания с нормальным межстрочным интервалом.

Если у объявления есть структурированные поля (год, марка, модель, метраж квартиры и т.п.):

- вывести их в отдельные «чипы» или таблицу:

  - пример:  
    - Год выпуска: 2019  
    - Память: 256 ГБ  
    - Состояние: Б/У  

================================
7. БЛОК «ПОХОЖИЕ ОБЪЯВЛЕНИЯ»
================================

- заголовок «Похожие объявления».
- горизонтальная карусель:

  каждая карточка:
  - маленькое фото (отношение 4:3),
  - цена,
  - короткий заголовок (1–2 строки),
  - город + расстояние.

Карточка кликабельна → открывает соответствующее объявление.

Использовать UI Kit (Card, Badge и т.п., если он уже есть).

================================
8. АДАПТИВ И UX
================================

1) При прокрутке:
   - шапка (navbar) может слегка изменяться:
     - логотип и название KETMAR, кнопка «Назад».
2) Все блоки должны иметь комфортные отступы (16–20px).
3) На маленьких экранах — без горизонтального скролла контента, только карусель похожих.

================================
9. ТЕХНИЧЕСКИЕ МОМЕНТЫ
================================

1) Компонент карточки разбей на логические части:
   - `AdGallery`
   - `AdHeaderInfo`
   - `AdSellerCard`
   - `AdDescription`
   - `AdSimilarCarousel`

2) Используй TypeScript и единый стиль из UI Kit.

3) Не ломать:
   - работу initData,
   - избранное (сердечко),
   - геоподсчёт расстояния.

Сделай из этой карточки «premium-опыт», максимально похожий по ощущениям на Avito Premium, но в брендинге KETMAR.