Скопируй в Replit целиком 👇

Сделай доработку для Ketmar MiniApp: нормальный визуал и UX админ-панели в профиле + создание/модерация магазинов.

0. Контекст

Это Telegram MiniApp (mobile-first).

Роли: user, seller, farmer, admin, moderator, super_admin.

Аккаунт 374243315 (proService) уже имеет роль super_admin — он главный админ.

Уже есть:

Админ-панель в профиле: пользователи с ролями admin | super_admin | moderator видят блок с фиолетовой рамкой «Панель администратора».

Страница /admin/sellers (управление продавцами).

Страница /admin/farmers (управление фермерами).

API:

GET /api/admin/sellers — список продавцов.

GET /api/admin/farmers — список фермеров.

POST /api/admin/sellers/:id/verify — верификация продавца.

POST /api/admin/sellers/:id/block — блокировка/разблокировка.

POST /api/admin/sellers/:id/set-farmer — назначить фермером.

Не ломай существующие эндпоинты и логику, только расширяй.

1. Визуал и UX «Панель администратора» в профиле

Сделай красивый, однозначно заметный блок в профиле с фиолетовой рамкой и понятной навигацией:

Компонент AdminPanelCard (или аналог, если уже есть):

Отображается только для ролей admin | super_admin | moderator.

Дизайн:

Фиолетовая рамка/подсветка (в стиле остального UI миниаппа).

Заголовок: «Панель администратора».

Подзаголовок: «Управление продавцами, фермерами и магазинами».

Иконка шестерёнки/щит/корона (для супер-админа) — по возможности.

Внутри сделай 3 крупных кликабельных блока / кнопки:

«Продавцы» → переход на /admin/sellers.

«Фермеры» → переход на /admin/farmers.

«Заявки на магазины» → переход на новую страницу /admin/shop-requests.

Для каждой кнопки:

Заголовок + короткое описание.

Маленький бейдж-счётчик (например, количество заявок «на проверке»). Для начала можно показывать только количество новых заявок, если быстро реализуемо.

Добавь нормальные состояния:

Skeleton-loader при загрузке счётчиков.

Понятные hover/active-состояния (где уместно).

Адаптация под узкий экран (телефон) — всё должно читаться и кликается с первого раза.

2. UI страниц /admin/sellers и /admin/farmers

Сделай удобный интерфейс управления:

Шапка страницы:

Заголовок: «Продавцы» или «Фермеры».

Подзаголовок с кратким описанием.

Слева — заголовок, справа — быстрые фильтры и поиск (на мобиле фильтры можно спрятать в горизонтальный скролл).

Фильтры статуса (таб-бар / сегменты):

Все

На проверке

Верифицированы

Заблокированы

Фильтры должны работать без перезагрузки страницы (меняем query-параметр, подгружаем данные).

Поиск:

Инпут «Поиск по имени, телефону или ID».

Дебаунс 300–500 мс.

Поиск работает поверх текущего фильтра статуса.

Список/таблица продавцов:

Каждый элемент списка (или строка таблицы) должен содержать:

Аватар (если есть) или заглушку.

Имя / название магазина.

Роль/типы: бейджи «Фермер», «Магазин», «Частник» и т.п.

Статус: чип «На проверке», «Верифицирован», «Заблокирован».

Город / район.

Дата регистрации / последней активности (если есть).

Справа — блок действий в виде маленьких кнопок или иконок:

«Магазин» / «Посмотреть магазин» → открывает страницу магазина/профиля продавца.

«Верифицировать» / «Снять верификацию»

Под капотом: POST /api/admin/sellers/:id/verify.

Если уже верифицирован → показывать «Снять верификацию» (или соответствующую логику, если реализована).

«Заблокировать» / «Разблокировать»

Под капотом: POST /api/admin/sellers/:id/block.

Для /admin/sellers: кнопка «Назначить фермером»

Под капотом: POST /api/admin/sellers/:id/set-farmer.

UX мелочи:

При нажатии на любую кнопку действий:

Показать спиннер на кнопке.

По завершении — обновить статус в списке (без полной перезагрузки).

При ошибке — показать понятный toast/alert.

Поддержка пагинации/ленивой загрузки, если уже реализована — просто вписаться в существующую схему.

3. Вкладка «Магазин» для обычного пользователя (нет магазина)

Если пользователь заходит во вкладку «Магазин» и у него нет своего магазина (и нет активной заявки):

Показываем пустое состояние:

Иконка/иллюстрация.

Заголовок: «У вас ещё нет магазина».

Текст: «Создайте магазин, заполните данные и отправьте на модерацию. После одобрения вы получите расширенные возможности, как у фермеров».

Кнопка: «Создать магазин».

По нажатию на «Создать магазин» открываем мастер создания магазина (многошаговая форма).

4. Мастер создания магазина (заявка на магазин)

Сделай многошаговый wizard (примерно 4–5 шагов), используя те поля, которые мы уже описывали ранее для карточки магазина (ничего не ломай, используй/переиспользуй существующие компоненты и типы):

Шаг 1. Основное

Название магазина (обязательное).

Тип:

Фермер

Магазин

Сервис

Короткое описание (2–3 строки, лимит по символам).

Шаг 2. Локация

Автоопределение гео (как в объявлениях).

Поля:

Город

Район / населённый пункт

Адрес / ориентир

Используй тот же геокомпонент, что и в подаче объявлений (чтобы не плодить дубликаты).

Шаг 3. Контакты и график

Телефон (обязательный).

Доп. контакты: Telegram, WhatsApp, Instagram, сайт (по возможности).

График работы: либо готовые пресеты (ежедневно 9–21), либо поля начала/окончания + дни недели.

Шаг 4. Логотип и медиа

Загрузка логотипа.

Загрузка обложки (баннер).

3–5 фото магазина/витрины/товаров.

Использовать тот же лоадер и оптимизацию, что и для фото объявлений (сжатие, превью).

Шаг 5. Условия работы

Доставка: Да/Нет.

Самовывоз: Да/Нет.

Зона обслуживания (город/радиус).

Минимальный заказ (опционально).

Категории товаров (можно взять из существующего дерева категорий, как в объявлениях — отмечаем, в чём специализируется магазин).

Поведение мастера

Прогресс-бар / шаги сверху: 1–5.

Возможность перехода «Назад/Далее».

Валидация на каждом шаге (обязательные поля подсвечиваем).

Кнопка «Сохранить и выйти» может работать как локальный draft (сохранить в state/локальное хранилище до отправки).

Отправка заявки

На последнем шаге:

Кнопка «Отправить на модерацию».

По нажатию:

Отправляем данные на backend (создай/используй эндпоинт, например:
POST /api/shop-requests
с телом: все поля анкеты + userId).

После успешного ответа:

Показать экран «Заявка отправлена»

Заголовок: «Заявка отправлена на модерацию».

Текст, что админ рассмотрит заявку и после одобрения появится магазин с расширенным функционалом.

На вкладке «Магазин» больше не показывать мастер, а отображать read-only карточку «Заявка на проверке» со статусом.

Если у пользователя есть активная заявка:

При заходе во вкладку «Магазин» показывать:

Статус: «На проверке».

Основные данные магазина (название, город, контакты).

Текст: «Вы сможете редактировать магазин после одобрения модерацией».

5. Админ-модерация заявок на магазины

Сделай новый админ-экран: /admin/shop-requests.

Backend (если нужно — допиши)

Предусмотреть эндпоинты:

GET /api/admin/shop-requests?status=pending|approved|rejected

По умолчанию status=pending.

POST /api/admin/shop-requests/:id/approve

POST /api/admin/shop-requests/:id/reject

При approve:

Создаём/обновляем запись продавца:

Привязываем к userId.

Присваиваем нужную роль (seller и/или farmer, если тип фермер).

Ставим статус «Верифицирован».

Привязываем созданный магазин к этому пользователю.

Делаем так, чтобы его дальше было видно в GET /api/admin/sellers / GET /api/admin/farmers (в зависимости от типа).

При reject:

Сохраняем причину отказа (поле rejectReason).

Заявка уходит в статус rejected.

Frontend /admin/shop-requests

Заголовок: «Заявки на магазины».

Фильтры:

На проверке

Одобренные

Отклонённые

Список заявок:

В каждой карточке заявки:

Имя пользователя + ID.

Название магазина.

Тип: фермер/магазин/сервис.

Город/район.

Контакты (телефон).

Дата подачи заявки.

Кнопка «Подробнее» (по клику — разворачиваем/открываем модалку с полными данными).

Действия для статуса pending:

Кнопка «Одобрить»:

Подтверждение (модалка).

Вызов POST /api/admin/shop-requests/:id/approve.

После успеха — обновить статус в UI, показать toast «Магазин одобрен, пользователю выданы права продавца/фермера».

Кнопка «Отклонить»:

Модалка с полем «Причина отказа» (опционально, но желательно).

Вызов POST /api/admin/shop-requests/:id/reject.

Обновить список.

Для approved / rejected:

Показывать статус и (для rejected) — причину отказа.

6. Поведение после одобрения магазина (кабинет магазина)

Когда заявка пользователя одобрена:

На стороне пользователя:

Вкладка «Магазин» больше НЕ показывает «заявку на проверке».

Вместо этого — полноценный кабинет магазина:

Шапка: название магазина, логотип, бейдж «Официальный магазин» или «Фермер».

Краткая статистика (можно использовать готовые компоненты из фермерского кабинета):

просмотры,

клики,

количество активных объявлений,

базовая аналитика.

Блок с «крутыми фишками», похожими на фермерские:

участие в ярмарках,

подсказки цен,

базовые гистограммы/аналитика (если уже реализовано для фермеров).

Используй уже существующие компоненты/логики фермерского кабинета:

Если есть FarmerDashboard и прочие виджеты, переиспользуй их для магазина с небольшими правками, чтобы не дублировать код.

7. Контроль доступа

Все маршруты /admin/... должны быть защищены:

Только admin, moderator, super_admin.

На фронте:

Проверяй роль при отображении «Панели администратора» и при переходе на /admin/*.

На бэке:

Добавь/используй middleware проверки ролей для эндпоинтов /api/admin/....

Сделай всё аккуратно, не ломая текущие API и существующий функционал, максимально переиспользуя уже имеющиеся компоненты (формы, гео, загрузку фото, фермерский кабинет).