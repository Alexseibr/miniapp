# Codex/Replit Mega Prompt — «Мой магазин» + витрина продавца

Копируй весь блок ниже в Codex/Replit. Это финальный единый промт без повторов для разработки кабинета продавца KETMAR Market (Telegram MiniApp + Web/PWA + Node.js backend + TypeScript + Mongo/Postgres).

```
🔥 ЕДИНЫЙ МЕГА-ПРОМТ — «МОЙ МАГАЗИН» + ВИТРИНА ПРОДАВЦА (МИНИ-APP) + АНАЛИТИКА + ЯРМАРКИ

Ты — senior fullstack developer KETMAR Market (Telegram MiniApp + Web/PWA + Node.js backend + TypeScript + Mongo/Postgres).
Нужно реализовать полноценный кабинет продавца «Мой магазин» и автогенерацию мини-аппа/мини-лендинга для каждого магазина: публикации, модерация, ярмарки, аналитика, антифрод, deep-links.

Глобальные цели
- Продавец создаёт магазин → появляется вкладка «Мой магазин» в MiniApp.
- После одобрения магазин получает: публичный URL (/shop/:slug), deep-link WebApp (start=shop_<id>), мини-лендинг (витрина), товары/публикации, аналитику, ярмарки. Каждый магазин — мини-апп внутри общего MiniApp.

Технологии
- Backend: Node.js + TypeScript, Mongo/Postgres, отдельные модули (shops, productTemplates, listings, analytics, fairService, publicShopPage, deepLinkDispatcher).
- Frontend: React + Telegram MiniApp API + PWA. Индексы по shopId/listingId/ts. Не оборачивать импорты в try/catch.

1. Финальная модель данных (TypeScript)
- Shop
  - status: 'pending' | 'approved' | 'rejected' | 'paused'
  - analyticsPlan: 'free' | 'pro' | 'max'
  - поля: id, ownerUserId, name, description, slug (уникален, можно менять 1 раз), logoUrl?, coverUrl?, slogan?, instagram?, website?, telegramUsername?, phone?, unp?, ownerFullName?, location { lat, lng, city, district?, addressLine? }, status, isVerified?, verificationRequested?, verificationAt?, analyticsPlan, createdAt, updatedAt.
- ProductTemplate
  - id, shopId, title, description, photos (<=4), categoryId, tags?, seasonCode?, createdAt, updatedAt.
- Listing
  - id, shopId, productTemplateId, price, quantity, status: 'active'|'paused'|'finished', fairId?, location { lat, lng, city, district? }, createdAt, updatedAt.
- Events
  - ViewEvent { id, listingId, shopId, userId?, ts, location? { lat, lng, city?, district? } }
  - FavoriteEvent { id, listingId, shopId, userId, ts }
  - ContactViewEvent { id, listingId, shopId, userId, ts }
- Complaints/Ratings
  - Complaint { id, listingId, shopId, fromUserId, reason: 'fake_product'|'wrong_price'|'spam'|'rude_seller'|'other', comment?, ts }
  - Rating { id, targetType: 'listing'|'shop', targetId, fromUserId, value: 1|2|3|4|5, reasonCode?, ts }
- Fair
  - { id, code, title, description, startAt, endAt, isActive, bannerImageUrl?, colorTheme?, allowedCategoryIds?, allowedSellerTypes?: ('shop'|'farmer'|'individual')[] }
- ShopFairParticipation
  - { id, shopId, fairId, status: 'pending'|'approved'|'rejected'|'active'|'finished', specialNote?, preferredDelivery?: 'pickup'|'courier'|'both', createdAt, updatedAt }
- ShopPageViewEvent
  - { id, shopId, userId?, ts, source?: 'telegram'|'instagram'|'direct'|'other' }

2. UX: вкладка «Мой магазин» в MiniApp
- Состояния
  - Нет магазина → большая плашка «Создай свой магазин», текст «Размещай товары, запускай ярмарки, получай аналитику», кнопка «Открыть магазин».
  - Pending → «Магазин на модерации» с данными и возможностью редактировать.
  - Approved → логотип/название, статус «Активен», кнопки: «Кабинет магазина», «Поставить магазин на паузу», «Моя витрина (мини-апп)».

3. Мастер создания магазина (3 шага)
- Шаг 1: название, описание, логотип, Instagram, Telegram/phone (подтягиваем), УНП/ФИО (необяз.).
- Шаг 2: локация без карты, автоопределение города/района, показываем текстом.
- Шаг 3: чекбокс «Согласен с правилами», кнопка «Отправить на модерацию». Создаётся Shop{ status: 'pending' }.

4. Админка: модерация + верификация
- Действия: approve/reject(with comment)/pause, назначить isVerified=true, видеть жалобы/рейтинги.
- Верификация через бота: /verify_shop ставит verificationRequested; админ подтверждает в админке.

5. Товары и публикации
- ProductTemplate: до 4 фото, без цены/количества.
- Массовая публикация: вкладка «Публикации» — выбираем шаблоны, вводим цену/количество, одной кнопкой создаём пачку Listing; локация берётся из Shop.location. Список объявлений: пауза/возобновить/завершить.

6. Аналитика магазина
- Overview: просмотры, избранное, контакты, конверсия, динамика просмотров по дням, топ товаров, география (районы/города), расстояния покупателей (0–2/2–5/5–10/10+ км).
- По объявлению: график, расстояния, оценки (гистограмма), жалобы, цена vs рынок.

7. Антифрод
- SellerQualityScore (0–100): учитывает рейтинг, жалобы/1000 просмотров, price spikes, повторяющиеся фейки, suspicious гео-паттерны.
- Админка: список магазинов по score, красные флаги, возможность ставить магазин на паузу.

8. Ярмарки
- Вкладка «Ярмарки»: Доступные / Мои участия / Архив.
- Процесс участия: кнопка «Участвовать» → согласие с условиями → описание доставки → заявка ShopFairParticipation(pending) → админ одобряет → active.
- Товары на ярмарке: выбираем шаблоны, вводим ярмарочную цену/лимит количества, создаём Listing{ fairId }.
- Аналитика ярмарки: просмотры, контакты, топ товаров, динамика, география.

9. Публичная витрина (мини-лендинг)
- URLs: https://<domain>/shop/:slug или /s/:shopId; editable slug (1 раз), кнопка «Скопировать ссылку», QR-код.
- Deep-link WebApp: https://t.me/<bot>?start=shop_<shopId> → грузит публичную витрину магазина.
- Контент витрины: шапка (логотип, cover, название, слоган, город/район, бейдж «Проверенный»), контакты (Telegram, телефон, Instagram, сайт), список активных Listing (новее → старее) с фото/описанием/ценой/кнопкой «Написать продавцу».
- Статистика витрины: ShopPageViewEvent с source (telegram/instagram/direct/other).
- Панель настройки витрины в кабинете: баннер, слоган, описание, редактирование slug, deep-link, «Скачать QR-код».

10. Требования к реализации
- Разделить модули: shops, productTemplates, listings, analytics, fairService, publicShopPage, deepLinkDispatcher.
- Индексация: shopId, listingId, ts. Телеграм MiniApp должен уметь загружать витрину по start=shop_<id>.
- БД: Mongo/Postgres, хранение сущностей по моделям выше.

11. QR-коды для магазинов
- Модуль `/modules/qr` (`qr.controller.ts`, `qr.router.ts`, `qr.service.ts`, `qr.storage.ts`), использовать `qrcode`/`qr-image`, PNG по умолчанию, SVG по запросу, кэширование и сохранение в хранилище.
- Модель `ShopQrCode { id, shopId, type: 'public_url'|'deep_link', format: 'png'|'svg', url, createdAt }`, индексы по `shopId` и паре (`shopId`,`type`,`format`).
- API: `POST /shops/:shopId/qr` (генерация для `/shop/<slug>` или `t.me/<bot>?start=shop_<id>`), `GET /shops/:shopId/qr`, `GET /shops/:shopId/qr/:qrId`, `DELETE /shops/:shopId/qr/:qrId`.
- В кабинете: блок “Ссылки и QR” с выбором типа/формата, скачать/скопировать/удалить; бот по `/myshop` может выдавать или создавать QR (кнопки “QR для витрины” / “QR для Telegram-миниаппа”).
- Опционально: `POST /admin/fairs/:fairId/qr` (QR на витрину с `?fair=<fairId>`), будущий `POST /shops/:shopId/listings/:listingId/qr`.

12. Связка Admin Panel + бот
- Бот принимает `POST /bot/admin-notify` на события `shop_created`, `verification_requested`, `complaint_new`, `seller_score_low`, и шлёт сообщения в админ-чат с кнопками.
- Callback-и `shop_approve_<id>`, `shop_reject_<id>`, `shop_pause_<id>`, `shop_unpause_<id>`, `shop_verify_<id>`, `shop_unverify_<id>` вызывают backend `POST /admin/shops/:id/approve|reject|pause|...`, бот отвечает подтверждением.
- В карточке магазина в админке: тумблер “Уведомлять администраторов в Telegram?” определяет отправку на `/bot/admin-notify`.
- Админ-команды: `/shops_pending`, `/complaints_today`, `/suspicious` (списки с inline-кнопками и ссылками в админку).
- Бот уведомляет продавца при approve/reject/verify/unpause: “Ваш магазин <name> одобрен/отклонён/верифицирован...”.
```
