# МЕГАПРОМТЫ ДЛЯ KETMAR MARKET: “Мой магазин” + витрина + ярмарки + аналитика

Ниже три отдельных мегапромта, готовых для отправки в Replit/Codex по очереди.

---

## 🟦 МЕГАПРОМТ №1 — BACKEND API
Ты — **senior backend developer** (Node.js + TypeScript + Express/Fastify + MongoDB/Postgres) в проекте **KETMAR Market**. Нужно спроектировать и реализовать backend API для модулей:

- “Мой магазин” (Shop + кабинет)
- Карточки товаров (ProductTemplate)
- Публикации (Listing)
- Публичная витрина магазина (public shop page)
- Ярмарки (Fair)
- Аналитика (events + отчёты)
- Антифрод и верификация

Фронт (MiniApp/WebView) уже использует эти эндпоинты.

### 1. Общие требования
- Везде TypeScript (типизация моделей и сервисов).
- Структура проекта по модулям: `/modules/shops`, `/modules/productTemplates`, `/modules/listings`, `/modules/fairs`, `/modules/analytics`, `/modules/publicShop`, `/modules/auth` (переиспользует существующую Telegram/phone auth).
- Авторизация: все кабинетные операции — только для залогиненного пользователя; для многих эндпоинтов проверяем `shop.ownerUserId === currentUser.id`.
- Публичные эндпоинты (витрина, просмотр объявления, события) доступны без авторизации, `userId` опционален.

### 2. API для магазинов (Shop)
**Модель**: как в мегапроме (name, description, slug, logo, location, status, isVerified, и т.д.).

**Создание** — `POST /shops`
- Body: `name`, `description`, `logoUrl?`, `instagram?`, `phone?`, `telegramUsername?`, `unp?`, `ownerFullName?`, `location { lat, lng }` (остальное дозаполняем).
- Генерируем: `slug` (по name, уникальный), `status = 'pending'`, `analyticsPlan = 'free'`.
- Ограничение: один магазин на пользователя.

**Получить магазин текущего пользователя** — `GET /me/shop` (404/null если нет).

**Обновление владельцем** — `PATCH /shops/:shopId`
- Только владелец. Можно менять: `description, logoUrl, instagram, website, phone, ownerFullName, location, coverUrl, slogan`.
- Нельзя: `ownerUserId`, `status`, `isVerified` напрямую. При `rejected` изменение может вернуть в `pending` (по правилу/флагу).

**Пауза/возобновление** — `POST /shops/:shopId/pause`, `POST /shops/:shopId/resume`
- Только владелец. При паузе `status = 'paused'`, опционально переводим все listings в `paused`.

**Админ**
- `GET /admin/shops?status=` (pending|approved|rejected|paused)
- `GET /admin/shops/:shopId`
- `POST /admin/shops/:shopId/approve` → `status = 'approved'`
- `POST /admin/shops/:shopId/reject` (body: `moderationComment`) → `status = 'rejected'`
- `POST /admin/shops/:shopId/verify` → `isVerified = true`, `verificationAt = now()`
- `POST /admin/shops/:shopId/unverify`

### 3. Витрина магазина (slug, ссылки, QR)
**Изменение slug** — `POST /shops/:shopId/slug` body: `{ slug }` с проверкой уникальности; можно ограничить 1 попыткой (`slugChangeCount`).

**Публичное получение магазина** — `GET /public/shops/:slugOrId`
- Без авторизации, возвращает публичный профиль + список активных товаров (пагинация, фильтр по категории).

**Событие просмотра витрины** — `POST /public/shops/:shopId/view` body: `{ source?: 'telegram' | 'instagram' | 'direct' | 'other' }` → создаёт `ShopPageViewEvent`.

### 4. ProductTemplate (карточки товаров)
- **Создание** — `POST /shops/:shopId/product-templates` (owner only) body: `title, description, photos[], categoryId, tags?, seasonCode?`.
- **Список** — `GET /shops/:shopId/product-templates`.
- **Изменение/удаление** — `PATCH /shops/:shopId/product-templates/:templateId`, `DELETE /shops/:shopId/product-templates/:templateId` (владение).

### 5. Listings (публикации)
**Массовое создание** — `POST /shops/:shopId/listings/bulk`
- Body: `{ items: { productTemplateId, price, quantity, fairId? }[] }`
- Проверяем принадлежность всех шаблонов магазину; подставляем `Shop.location`; создаём `Listing` на каждый.

**Список публикаций** — `GET /shops/:shopId/listings?status=active|paused|finished&fairId=...`

**Статусы** — `POST /shops/:shopId/listings/:listingId/pause|resume|finish`.

### 6. Публичные объявления
- `GET /public/listings/:listingId` — публичная карточка + ограниченные shop-данные.
- События: `POST /public/listings/:listingId/view` (ViewEvent), `/favorite` (FavoriteEvent), `/contact-view` (ContactViewEvent). `userId` опционален.

### 7. Ярмарки (Fair)
**Админ**: `POST /admin/fairs`, `PATCH /admin/fairs/:fairId`, `GET /admin/fairs`, `GET /admin/fairs/:fairId`.

**Публичный список доступных** — `GET /fairs/available-for-me` (по текущему shop и sellerType/статусу).

**Участие магазина** — `POST /shops/:shopId/fairs/:fairId/participations`, `GET /shops/:shopId/fairs/participations`, `GET /shops/:shopId/fairs/:fairId/participation`.

**Админ модерация участия** — `POST /admin/fairs/:fairId/participations/:participationId/approve|reject`.

### 8. Аналитика
Сервис `analyticsService` + REST-обёртки.

- **Обзор магазина** — `GET /shops/:shopId/analytics/overview?from=&to=` → `totalViews`, `totalFavorites`, `totalContactViews`, `dailySeries`, `topListingsByViews`, `geoDistribution`, `distanceBuckets`.
- **По объявлению** — `GET /shops/:shopId/listings/:listingId/analytics?from=&to=` (аналогично).
- **Витрина** — `GET /shops/:shopId/analytics/shop-page?from=&to=` (агрегирует `ShopPageViewEvent`).
- **Ярмарка** — `GET /shops/:shopId/fairs/:fairId/analytics`.

### 9. Антифрод и качество
Сервис `sellerQualityService`: периодический job считает `SellerQualityScore` (rating, complaints/1000 views, price spikes, фейки, suspicious geo). Сохраняет/кеширует в коллекции `SellerQualityScore` (`shopId -> score, flags`).

**Админ API** — `GET /admin/sellers/quality?minScore=&maxScore=&flag=...`, `GET /admin/shops/:shopId/quality`.

---

## 🟣 МЕГАПРОМТ №2 — UI/UX
Ты — **senior product/UX/UI designer + фронтенд-лид**. Спроектируй экраны и UX-потоки для модулей:

- “Мой магазин” в профиле
- Мастер создания магазина
- Кабинет магазина
- Витрина магазина (публичный мини-лендинг)
- Ярмарки внутри магазина
- Аналитика (простые, читаемые графики)
- Антифрод-элементы в админке (общие штрихи)

**Стиль**: тёмная тема, неон/матрица, аккуратные карточки. Минимум текста, максимум понятных CTA. Mobile-first, WebView.

### Главный сценарий (профиль)
- Блок “Мой магазин”:
  - **Нет магазина**: яркая CTA-плашка с иллюстрацией, 2–3 буллета преимуществ, кнопка “Открыть магазин”.
  - **Есть магазин**: логотип, название, статус-чип (зелёный “Активен” / жёлтый “На модерации” / серый “Пауза”), две крупные кнопки: “Кабинет магазина”, “Моя витрина (мини-страница)”.

### Мастер создания магазина (3 шага)
- Шапка: прогресс-бар (1/3, 2/3, 3/3).
- **Шаг 1**: карточки ввода, placeholder-ы и подсказки, лимиты по длине.
- **Шаг 2**: большая иконка гео, текст “Мы определили ваш город: … район: …”, маленькая ссылка “Изменить вручную”.
- **Шаг 3**: резюме (мини-карточка магазина), чекбокс, кнопка “Отправить на модерацию”.

### Кабинет магазина — общий layout
- Верхняя шапка: логотип, название, статус, бейдж “Проверенный магазин”.
- Табы: “Обзор”, “Товары”, “Публикации”, “Ярмарки”, “Аналитика”, “Витрина”.

**Обзор**: 2–3 ключевые цифры, карточка статистики, последние 3 объявления.

**Товары**: список карточек (фото+название), FAB “+ Добавить товар”; пустое состояние с иллюстрацией.

**Публикации**: переключатель “Активные / На паузе / Завершенные”; масс-режим — карточки с чекбоксами, нижняя панель: поля “цена, количество, кнопка ‘Опубликовать’”; подсказка для пустоты.

**Ярмарки**: верхний блок “Доступные ярмарки” (горизонтальный скролл баннеров), ниже “Мои ярмарки” (табличный список); для участия — экран “Товары ярмарки” (чекбоксы шаблонов, нижняя панель “Выбрано X → Указать цены/лимиты → Опубликовать”), бейджи “Ярмарка” на товарах.

**Аналитика**: график “просмотры по дням”; блок “Где вас смотрят?” (гистограмма по районам); блок “Расстояние до покупателей” (гистограмма 0–2 / 2–5 / 5–10 / 10+ км).

**Витрина**: предпросмотр мини-лендинга в рамке; блок “Ссылки и QR” (URL, deep-link, кнопка “Скачать QR”).

### Публичная витрина магазина
- Верхний блок: баннер/cover, круглый логотип, название, город/район, бейдж “Проверенный”.
- Раздел “О магазине”: короткое описание, слоган.
- Раздел “Контакты”: кнопка “Написать в Telegram”, “Позвонить”, “Instagram”.
- Раздел “Товары”: вертикальная лента карточек (фото 1:1, название, цена, бейдж “Ярмарка” если `fairId`).
- Низ: логотип KETMAR + текст “Магазин на платформе KETMAR Market”.

### UX подсказки / пустоты
- Нет товаров → иллюстрация + “Добавьте первый товар”.
- Нет публикаций → “Выберите товары и опубликуйте их в один клик”.
- Нет ярмарок → “Подключайтесь к сезонным ярмаркам, чтобы получить больше клиентов”.

### Навигация и компоненты
Опиши экраны так, чтобы Codex мог сгенерировать React-компоненты: header, карточки, табы, FAB, списки, рамка предпросмотра, CTA-кнопки, переключатели статусов, чекбоксы для масс-режима.

---

## 🟢 МЕГАПРОМТ №3 — TELEGRAM-БОТ
Ты — **senior backend/бот-разработчик**. Расширь Telegram-бот для deep-links, верификации и выдачи ссылок витрины. Бот: `@KetmarM_bot`, WebApp: `https://<domain>/miniapp`.

### 1. Deep-link `start=shop_<shopId>`
- В `/start` обрабатываем `ctx.startPayload`. Если `shop_`: парсим `shopId`, отвечаем приветствием “Вы перешли в магазин <название> на платформе KETMAR Market”, добавляем web_app кнопку с URL `https://<domain>/miniapp?shopId=<shopId>` (или через initData). В MiniApp при инициализации, если есть `shopId`, сразу открываем публичную витрину магазина.
- `/start` без payload → общий сценарий.

### 2. Команда `/myshop`
- По Telegram ID через backend ищем `Shop.ownerUserId`.
- Если магазина нет: сообщение “У вас ещё нет магазина. Откройте его через MiniApp в разделе Профиль → Мой магазин” + web_app кнопка в профиль.
- Если есть: получаем `name, slug, status, isVerified`; формируем `URL: https://<domain>/shop/<slug>`, `deep-link: https://t.me/<bot>?start=shop_<id>`.
- Отправляем карточку: “Ваш магазин: <name>”, статус, кнопки: “Открыть кабинет” (WebApp → профиль/кабинет), “Ссылка на витрину” (inline URL), “Скопировать deep-link” (текст), “QR-код” (картинка, если backend генерирует png).

### 3. Команда `/verify_shop`
- Проверяем наличие магазина. Если нет — “Сначала создайте магазин”.
- Если есть — чек-лист готовности (логотип, заполненные данные), спрашиваем: “Отправить запрос на верификацию?” (Yes/No). По “Да” → backend `POST /shops/:shopId/verification-request` (`verificationRequested = true`); бот: “Запрос отправлен. Сообщим о решении”.
- Уведомления: когда backend ставит `isVerified = true`, вызывает бота (webhook/HTTP), бот отправляет: “Ваш магазин <name> прошёл верификацию ✅”.

### 4. Уведомления о модерации
- Backend дергает бота при approve/reject: `tgUserId, shopId, status, comment?`.
- Бот шлёт: при approve — “Магазин <name> одобрен…”. При reject — “Отклонён. Причина: <comment>”.

### 5. Структура бота (TypeScript + Telegraf/grammY)
- Логику работы с backend вынести в `apiClient`: `getMyShopByTelegramId(tgId)`, `getShopPublicLinks(shopId)`, `requestShopVerification(shopId)`, и т.д.
- Команды: `/start [payload]`, `/myshop`, `/verify_shop`, `/help` (коротко описать функционал).
- Кнопки: web_app для MiniApp; inline-кнопки для ссылок и подтверждения.
